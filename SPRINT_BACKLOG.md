# Sprint Backlog

งานของ sprint ที่กำลังทำอยู่ ติ๊กแล้วคือ ship แล้ว

ว่างอยู่ — Phase 14.1 (Character คุยกันเอง) และ 14.2 (ทำงานร่วมกันได้) ship แล้วทั้งคู่
ย้ายไปอยู่ `PRODUCT_BACKLOG.md` หัวข้อ "Phase 14-1 / 14-2" ตามกติกาใน charter
หัวข้อ "Where the backlog lives" — ลงมือแล้วเป็นกลไกเดียวกัน จึงบันทึกรวมเป็นหัวข้อเดียว

ไล่ตามลำดับที่ ship: v0.14.229 (คุยกัน ส่งงาน รับคำตอบ) → v0.14.235 (แก้ 4 บั๊ก
รวมบั๊กที่โมเดลไปใช้ `SendMessage` ของ Claude Code แล้วรายงานเท็จว่าส่งสำเร็จ) →
v0.14.239 (ส่งหลายคนพร้อมกันแบบ parallel และทำ step ต่อเนื่อง) → v0.14.240
(บทสนทนาที่เป็นงานของตัวอื่นล้วนๆ ไม่ถูกเซฟ)

หยิบเฟสถัดไปมาจาก `PRODUCT_BACKLOG_NEXT_SPRINTS.md` — เฟสถัดไปตามลำดับคือ 14.3
(instruction / task log ร่วมกัน) ซึ่ง **ยังติดด่าน**: บรรทัด `## TODO: Design App
Architecture ก่อน` ที่หัวไฟล์นั้น เจ้าของต้องลบเอง

## Phase 14.3

ของที่ 14.3 ขอไว้บางส่วนมีแล้วจาก 14.2 และควรต่อยอด ไม่ใช่เขียนใหม่:

- [ ] **ทำ parallel ได้กี่ตัว / ต้องรออะไรจึงรู้ว่าเสร็จ** — `ErrandPlan` ทำอยู่แล้ว
  (ส่งพร้อมกัน รอครบ แล้วรันขั้นถัดไปที่ผู้สั่ง) พร้อม timeout และการตัดคนที่ติดต่อไม่ได้
- [ ] **กัน `ทำซ้ำกัน` / `infinite loop`** — มี hop limit 2, หนึ่งงานต่อคู่ต่อทิศทาง,
  และหมดอายุ 15 นาที ทุกข้อเป็น pure predicate ที่รับ `now:` เป็นพารามิเตอร์สุดท้าย
