# Product Backlog — AI Desktop Companion / AI Secretary

Feature list by phase, extracted out of `CLAUDE.md` so the charter (architecture,
principles, engineering rules) stays separate from the backlog (what's built vs.
what's left). Checked items are shipped; unchecked items are still open.

The phase digit in `AppVersion.swift` (`major.phase.change`) is **not** derived
from anything in this file — the highest heading here has never been the current
phase. It is stated once, in `CLAUDE.md` → Engineering expectations. A copy kept
here said **9** while the charter said **10**, so the copy is gone rather than
corrected.

## Phase 1: Desktop companion shell

- [x] Transparent floating character window.
- [x] Basic character state animations or placeholders.
- [x] Dragging and basic interaction.
- [x] Chat panel opened from the character.
- [x] Local mock state transitions.

## Phase 2: Secretary and coding workflow

- [x] Intent classification for a limited set of commands.
- [x] Project registry and explicit working-directory resolution.
- [x] Task lifecycle, approval prompts, execution logs, and result summaries.
- [x] Claude Code adapter for approved coding tasks.
- [x] Basic Git status/diff and test-result reporting.

## Phase 3: Chat with me

- [x] Integration with Claude as a mask app.
- [x] ใช้ Claude Code ที่ user ติดตั้งและ login ไว้แล้ว (บัญชีของ user เอง ไม่ต้องใช้ API key) — ถ้าเครื่องไม่มี ให้แนะนำวิธีติดตั้งและ login
- [x] context ต่อเนื่องเหมือน ChatGPT: ผลลัพธ์คำสั่งและเนื้อหาไฟล์ที่อ่านต้องอยู่ใน context เพื่อถามต่อเนื่องได้
- [x] จำ project ล่าสุดที่ทำงานอยู่ ไม่ต้องพิมพ์ `in <project>` ซ้ำทุกครั้ง
- [x] ถ้า add project มากกว่า 1 ต้องเข้าใจบริบทของทุก project พร้อมกัน
- [x] ต่อ local / localhost MCP ได้ โดยอ่าน config จาก Claude Code ของ user เอง ไม่ต้องตั้งค่าซ้ำในแอป
- [x] แสดงข้อความ activity ที่ AI กำลังทำ (กำลังคิด / เรียกเครื่องมือไหน) แทรกอยู่ในสายของ chat และมีกรอบข้อความ เพื่อให้เห็นชัดว่านี่ไม่ใช่คำตอบ
- [x] ปิด/เปิด activity ได้ด้วยการ toggle icon status และบอกผลการ toggle ในสาย chat ด้วยกรอบเดียวกัน จำค่าไว้ข้ามการเปิดแอป ครั้งแรก default ซ่อน
- [x] เวลา AI ตอบ chat ถ้า position อยู่ที่ bottom ให้ auto scroll ตาม แต่ถ้า user scroll ขึ้นไปอ่าน message เก่าๆ ไม่ต้อง auto scroll จนกว่า user จะ scroll ลงมาล่างสุด
- [x] แสดง markdown table เป็นตารางจริงใน chat ถ้าตารางกว้างเกินหน้าต่าง ให้ scroll แนวขวางได้เฉพาะ content ของตาราง ไม่ใช่ทั้ง chat

## Phase 4: Settings

- [x] ใช้ window chat เดิมได้ ไม่ต้องมีหน้าต่างใหม่
- [x] เพิ่ม increase / decrease font size (มีปุ่มแค่ + -) max ที่ 32
- [x] เพิ่ม increase / decrease ขนาดหน้าต่าง chat (มีปุ่มแค่ + -) แล้วหน้าต่างจะยืดหดได้ โดย minimum เท่าขนาด default และ max ไม่เกินขนาดหน้าจอ
  - [x] เดิมข้อนี้เขียนว่า "แนวตั้งเท่านั้น" — Phase 5.5 เปิดให้ปรับความกว้างด้วยแล้ว (ปุ่ม step และลาก grip)
- [x] เลือก model และ effort ได้จากหน้า setting (คลิกที่ชื่อแล้วมี popup ให้เลือก) โดย default ใช้ค่าเดียวกับ Claude Code ของ user และบอกการเปลี่ยนแปลงใน chat

## Phase 5: Secretary Profiles

- [x] ขยาย/ย่อ app size ได้ 3 ระดับ (S,M,L - เล็กลง/ใหญ่ขึ้น 30%) - ขนาดปัจจุบันคือ M
- [x] สร้าง profile ใหม่ได้
  - [x] ตั้งชื่อได้ ชื่อจะแสดงใน chat (เช่น ตอนนี้คือ Miku)
  - [x] upload รูป profile ใหม่ได้ (save ใน local storage)
  - [x] รูป profile จะแสดงใน bubble ของ app
  - [x] upload รูป แยกตาม activity ได้ เช่น Idle, Thinking, ... ซึ่งจะ require แค่รูปเดียวเป็น default ของ profile
  - [x] กำหนดเพศได้ เช่น เพศ หญิง/ชาย/LBGTQ+ (นอกจากชายหญิง กรอก free text ได้)
  - [x] กำหนดวัยได้ เด็ก/วัยรุ่น/ผู้ใหญ่ หรือกำหนดอายุเลย
  - [x] กำหนดสไตล์ได้ เช่น มืออาชีพ เพื่อน เป็น free text ถ้าไม่เข้าใจ ใช้ default คือ มืออาชีพ (ไม่อนุญาติให้สื่อไปทางความสัมพันธ์ทางเพศ)
- [x] เปลี่ยน profile ได้ App จะ refresh ทันที
- [x] App จะแสดงรูปที่แตกต่างกันตาม activity ที่กำลังทำ (เปลี่ยนเมื่อ thinking หรือ idle)
- [x] เมื่อ activity แต่ไม่มีรูป ให้ใช้รูป default รูปเดียว

## Phase 5.5: ปรับ UI/UX จากการใช้งานจริง

- [x] ครั้งแรกที่ run ต้องมี default profile คือ Miku เขียนลง local storage เลย ไม่ใช่มีแค่ใน memory
- [x] upload รูป profile รูปเดียวต่อ profile (ไม่แยกตาม activity) เป็นแถว Picture แถวเดียว คลิกแล้วมี popup (Choose / Clear) แบบเดียวกับ Model และ Effort — activity สื่อด้วยสี halo, badge และ label ใต้ตัวละคร
  - [x] ของเดิมที่ upload แยกตาม activity ไว้ ต้องย้ายมาเป็นรูปเดียวให้อัตโนมัติ ไม่ให้รูปหาย
- [x] ตัวละครต้องอยู่กลาง bubble ไม่ว่ารูปจะสัดส่วนแบบไหน
- [x] เอาปุ่ม Debug ออกจาก chat window
- [x] ตารางและกล่อง activity ต้องขยาย/ย่อตาม font size ที่ตั้งไว้
- [x] หน้าต่างตัวละครต้องขยายตาม S/M/L จริง และต้องไม่ถูกตัดขอบ (bubble ที่คลุมตัวละครต้องกลมครบวง)
- [x] ตำแหน่ง chat window ต้องห่างจากตัวละครตามสัดส่วนขนาด app (S ไม่ห่างเกิน L ไม่ทับกัน)
- [x] URL ใน chat ต้องคลิกเปิดเบราว์เซอร์ได้ ทั้ง URL ล้วนและ markdown link
  - [x] hover แล้ว cursor เปลี่ยนเป็นนิ้ว และขึ้น underline
  - [x] อนุญาตเฉพาะ http/https/mailto — scheme อื่น (เช่น file:, javascript:) แสดงเป็นข้อความธรรมดา คลิกไม่ได้ เพราะข้อความจาก model/เว็บ/tool ถือเป็น untrusted
- [x] คลิกที่ตัวละคร 1 ครั้ง = เปิด/ปิด chat 1 ครั้ง (คลิกแรกต้องไม่ถูกกินไปเป็นการ focus หน้าต่าง)
- [x] textbox ของ chat ต้อง word wrap และขยายทีละบรรทัด จาก 1 เป็น 2,3,4 สูงสุด 5 บรรทัด เกินนั้นไม่ขยายต่อ แต่ scroll แนวตั้งได้
  - [x] Enter = ส่ง, Shift+Enter หรือ Option+Enter = ขึ้นบรรทัดใหม่
- [x] resize chat window ได้เองด้วยการลาก grip ที่มุมบนด้านตรงข้ามหาง (อีกมุมเป็นแถวปุ่ม) ปรับได้ทั้งกว้างและสูงพร้อมกัน, minimum เท่าขนาด default, max ไม่เกินหน้าจอ
  - [x] ลากไปทางที่กล่องจะขยายออก (ขอบด้านหางถูกตรึงไว้กับตัวละคร กล่องจึงโตออกด้านตรงข้ามเสมอ)
  - [x] **resize คือ resize อย่างเดียว ห้ามขยับตัวละคร** — `applyWindowSizes` ทำงานทุกครั้งที่ appearance เปลี่ยน จึงห้ามเรียก `keepCharacterOnScreen` จากที่นั่นโดยไม่เช็คว่าขนาด *ของตัวละครเอง* เปลี่ยนไหม เคยวัดได้ว่าตัวละครที่ยืนคร่อม Dock กระโดดขึ้น 54pt ทันทีที่แตะกริป
    - [x] clamp ตัวละครกับ `screen.frame` ไม่ใช่ `visibleFrame` — ยืนบน Dock เป็นที่ปกติของ desktop character กติกาที่เหลือคือแค่ "ห้ามหลุดออกนอกจอ"
  - [x] **พลิกกล่องลงใต้ตัวละครเฉพาะตอนข้างล่างมีที่จริง** — กฎเดิมพลิกทันทีที่ "วางข้างบนไม่พอ" โดยไม่ถามว่าข้างล่างพอไหม กล่องสูง 883 เหนือตัวละครที่ยืนคร่อม Dock (จอ 1920×1080: ตัวละคร y 30–179) ข้างบนขาดไป 6pt แต่ข้างล่างเหลือ **−18** พอพลิกลงแล้วโดน clamp กลับเข้าจอ กล่องไปลงที่ y 62–945 คือกลบตัวละครทั้งตัว ซึ่งเป็นสิ่งเดียวที่ห้ามเกิด เพราะตัวละครคือทางเดียวที่ user เอื้อมถึงกล่อง — `placeBubble` (ใน `SecretaryCore` เพื่อให้เทสได้โดยไม่ต้องมีจอ) จึงเทียบที่ว่างสองด้าน พลิกเมื่อด้านล่างพอ หรือ (ถ้าไม่พอทั้งคู่) ด้านล่างว่างกว่า
    - ทางพลิกต้องยังใช้ได้ ตัวละครที่อยู่ติดแถบเมนูต้องได้กล่องอยู่ใต้ตัวและหางชี้ขึ้น — มีเทสคุมทั้งสองทิศ
    - ที่ความสูงสุด (= ความสูงจอ) กล่องยังทับตัวละครอยู่ดีประมาณ 115pt เพราะขนาดที่ตั้งไว้ใหญ่กว่าที่ว่างเหนือหัว เป็นผลตรงของกติกา "max ไม่เกินหน้าจอ" ไม่ใช่บั๊กของการวาง — ถ้าจะแก้ต้องลดเพดานความสูง ซึ่งเป็นการหั่นค่าที่ user ตั้งเอง ต้องให้เจ้าของเลือก
  - [x] **กริปต้องอยู่มุมที่กล่องโตออก ไม่ใช่มุมบนเสมอ** — พอกล่องพลิกไปอยู่ใต้ตัวละคร ขอบบนถูกหางตรึงไว้ กล่องโตลงล่าง แต่กริปยังค้างอยู่มุมบน คนจึงต้องลากลง "ผ่าน" กริปที่ไม่ตามเมาส์ และลากเข้าหาตัวละคร ทั้งที่พื้นที่ว่างที่กำลังจะถมอยู่อีกฟากของกล่อง — กฎมุมอยู่ใน `GripCorner` (แนวนอน = ตรงข้ามหาง, แนวตั้ง = ขอบที่ขยับได้) มีเทสคุมทั้ง 4 กรณี ไม่ใช่แค่ 2 กรณีที่เปลี่ยน
    - แถวปุ่ม Settings/Profile/Projects อยู่มุมล่างด้วย จึงต้องย้ายไปอีกฝั่งเมื่อกริปมาลงมุมเดียวกัน ไม่งั้นกริปทับปุ่ม Settings
    - glyph เลือกด้วย**ชื่อ symbol ตามแนวทแยงของมุมนั้น** ไม่ใช่หมุนอันเดียว และต้องเป็นตัว "กางออก" — ทุกแนวทแยงมีฝาแฝดที่ลูกศรชี้เข้าหากัน (`arrow.up.right.and.arrow.down.left` = หุบเข้า) ทั้งคู่เป็น symbol ที่มีจริง จับได้ทางเดียวคือเปิดดูของจริง มีเทสกันทั้งชื่อที่ไม่มีอยู่ (render เป็นความว่าง ดูเหมือนกริปหาย) และฝาแฝดที่ชี้เข้า
  - [x] ทิศทางการโตต้องตรึงไว้ตั้งแต่เริ่มลาก (เก็บใน `DragOrigin`) ห้ามอ่านสดจาก layout ทุก event — พอกล่องไม่พอที่ด้านบนมันจะพลิกไปอยู่ใต้ตัวละครแล้วทิศกลับด้าน ลากค้างทางเดิมกลายเป็นย่อ แล้วพลิกกลับ วัดได้ 909 → 801 → 933 → 777 ใน 4 event และแกว่งกว้างขึ้นเรื่อยๆ
- [x] มี icon "←|→" (กว้างขึ้น) และ "→|←" (กลับเป็นขนาด default) เรียงข้างปุ่มปิด chat window โดย icon ทั้งสองเล็กกว่าปุ่มปิด 30%
  - [x] "←|→" กด 1 ครั้ง = 1 step: x1 → x2 → x3 แล้ว disable
  - [x] "→|←" กดครั้งเดียวกลับเป็นขนาด default ทันที ไม่ต้อง step
- [x] เมื่อ mirror กล่องคำพูด ให้ grip กับแถวปุ่ม (ปิด/ย่อ/ขยาย) สลับด้านกัน และสลับลำดับปุ่มด้วย โดยปุ่มปิดอยู่ริมนอกสุดเสมอ
- [x] Title ของกล่อง chat อยู่ใต้แถวปุ่ม ชิดซ้ายเสมอ ไม่ทับปุ่มและไม่ขยับตามการ mirror
- [x] ขยาย/ย่อ font ด้วย ⌘+ และ ⌘− ได้ ไม่ใช่แค่ปุ่ม + − ใน Settings
- [x] font size ต้องมีผลกับปุ่ม Settings/Profile/Projects และตัวอักษรในช่องพิมพ์ข้อความด้วย ไม่ใช่แค่เนื้อหา chat
  - [x] ตัวอักษรในช่องพิมพ์ต้องอยู่กลางกล่องตามแนวตั้ง ไม่ลอยติดขอบบน
- [x] เวลาขยาย/ย่อ chat window ปลายแหลมของกล่องคำพูดต้องอยู่ตำแหน่งเดิม (ยึดขอบด้านหาง แล้วขยายออกด้านตรงข้าม) ทั้งกรณีหางอยู่ซ้ายและหางอยู่ขวา (mirrored)

## Phase 5.6: Version and About

- [x] เลข version อยู่ใน code (`SecretaryCore/AppVersion.swift`) เป็นแหล่งเดียว — `package-app.sh` อ่านค่านี้ไปใส่ `CFBundleShortVersionString` ไม่ให้เลขสองที่ไม่ตรงกัน
- [x] แอปต้องบอก version ตัวเองได้ แม้รันแบบไม่มี bundle
- [x] มีหน้าต่าง About เปิดจากเมนู status bar แสดงชื่อ, version, และคำอธิบายสั้นๆ
- [x] ⌘H = Hide/Show Character (ไม่ใช่ hide ทั้งแอป เพราะ accessory app ไม่มีหน้าต่างใน Dock ให้เรียกกลับ)
  - [x] ซ่อนคือ "เอาไปพ้นทางแป๊บนึง" ไม่ใช่ค่าที่ต้องจำ — เปิดแอปอีกครั้งต้องเห็นตัวละครเสมอ ต้องปิดสองทางถึงจะครบ ทั้งคู่เคยทำให้เปิดแอปแล้วไม่มีอะไรขึ้นเลย
    - [x] เปิดแอปซ้ำตอนตัวเดิมยังรันอยู่ macOS **ไม่ได้ launch process ใหม่** แต่ส่ง `applicationShouldHandleReopen` มาแทน — ถ้าไม่ดักไว้ การดับเบิลคลิกแอป (ซึ่งเป็นสิ่งที่คนทำตอนหาตัวละครไม่เจอ) จะไม่เกิดอะไรขึ้นเลย
    - [x] panel ต้อง `isRestorable = false` ไม่งั้น state ที่ AppKit จำไว้ตอน quit สั่ง orderOut ทับ `orderFrontRegardless` ใน `applicationDidFinishLaunching` ได้
- [x] **Esc ปิดกล่องแชทได้แม้แอปไม่ได้เป็นหน้าต่างหน้าสุด**
  - `addLocalMonitorForEvents` กับ key equivalent ของเมนู เห็นเฉพาะอีเวนต์ที่ระบบเลือกส่งมาให้แอปนี้ — กล่องลอยอยู่เหนือแอปอื่น กด Esc คีย์ไปที่แอปนั้น กล่องจึงไม่ปิด "โค้ดถูก แต่คีย์ไม่เคยมาถึง" แบบเดียวกับ `.onExitCommand`
  - ใช้ `RegisterEventHotKey` ของ Carbon — ทางเดียวที่ไม่ต้องขอสิทธิ์ Accessibility (ต่างจาก `CGEventTap`) และเป็นทางเดียวที่ **กลืน** คีย์ ส่วน `addGlobalMonitorForEvents` ได้ยินแต่กลืนไม่ได้ แอปที่อยู่หน้าสุดจะทำตามคีย์นั้นไปด้วย
  - **จดทะเบียนเฉพาะตอนกล่องแชทเปิด กล่องปิดแล้วไม่ยึดอะไรเลย** — Esc เป็นคีย์ที่คนใช้ตลอด (ปิด dialog, ออก full screen, จบสไลด์โชว์) ยึดค้างไว้ทั้งที่กล่องไม่ได้เปิดคือพังของทั้งเครื่องแลกกับหน้าต่างที่ไม่ได้โผล่
  - กติกาว่ายึดคีย์ไหนเมื่อไหร่อยู่ใน `GlobalShortcut` ที่ `SecretaryCore` มีเทสคุม ไม่ใช่ register สองบรรทัดฝังในโค้ดหน้าต่าง — ทุกคีย์ที่ยึดคือคีย์ที่หายไปจากทุกแอปบนเครื่อง ของแบบนี้ต้องเห็นได้ในที่เดียว
- [x] **⌘H ต้องเป็นชอร์ตคัตของแต่ละแอป ห้ามยึดทั้งเครื่อง** — เคยยึดไปแล้วหนึ่งรอบ (0.6.61) ผลคือ Hide ของทุกแอปบนเครื่องหายไป เจ้าของแจ้งภายในไม่กี่นาที คืนให้ใน 0.6.62
  - บทเรียนไม่ใช่แค่ "⌘H พิเศษ" แต่คือ **คอมบิเนชันที่มี ⌘ เป็นของเมนูแอปอื่นเสมอ** — มีเทสบังคับว่าชอร์ตคัตที่ยึดได้ห้ามมี Command เป็นตัวประกอบ เรื่องนี้จึงกลับมาโดยบังเอิญไม่ได้
  - ถ้าจะเอา "ซ่อนทั้งแอปจากที่ไหนก็ได้" จริงๆ ต้องเป็นคอมบิเนชันที่ไม่มีใครใช้ เช่น ⌥⌘H ไม่ใช่ทับของเดิม
  - ตอนถามเจ้าของให้เลือก ตัวเลือกที่เขียนให้เลือกก็ต้องไม่ขัด requirement ที่เคยให้ไว้ — รอบนั้นเขียนตัวเลือก "ยึดทั้งระบบ" ขึ้นมาเอง แล้วก็ทำตามตัวเลือกที่ผิดนั้น
- [x] **dialog เลือกโฟลเดอร์/รูป ต้อง `NSApp.activate` ก่อน `runModal()`** — `runModal()` เปิดหน้าต่างให้ แต่ไม่ได้ดึงแอปขึ้นมาเป็นแอปหน้าสุด และตอนกดปุ่มแอปมัก**ไม่ได้**เป็นแอปหน้าสุดอยู่แล้ว เพราะแชทเป็น non-activating panel คลิกในนั้นไม่ทำให้แอป active — open panel ของแอปที่ไม่ active จะไม่เป็น key window รายการโฟลเดอร์เลยไม่ตอบสนองการคลิก นี่คือเหตุผลของคำว่า "บางครั้ง": มันใช้ได้ถ้าเพิ่งเปิดแชท เพราะการเปิดแชทเรียก activate อยู่แล้ว
  - ไม่ใช่เรื่องระดับหน้าต่างบัง — วัดแล้ว dialog อยู่ layer 8 (`.modalPanel`) ส่วน panel ของเราอยู่ layer 3 (`.floating`) มันอยู่เหนือเราอยู่แล้ว
  - **ต้องคืน hotkey ระหว่าง modal ด้วย** (`GlobalHotKeys.whileSuspended`) — Carbon hotkey ยิงไม่สนว่าอะไรอยู่บนจอ พอกล่องแชทเปิดอยู่หลัง dialog กด Esc จะไปปิดแชทแล้วทิ้ง dialog ค้างไว้ ซึ่งเป็นปุ่มที่ทุกคนใช้ยกเลิก dialog และต้อง "คืนคีย์" ไม่ใช่ "เมินคีย์" เพราะเมินแล้วคีย์ยังถูกกลืนอยู่ดี กด Esc จะไม่เกิดอะไรเลย
- [x] **อย่าใช้การ register ซ้ำเป็นเครื่องมือวัดว่าใครถือคีย์อยู่** — เขียนโพรบไปแล้วรอบหนึ่ง macOS ยอมให้อีก process จดคีย์เดียวกันได้ มันตอบ FREE ทั้งตอนที่แอปถืออยู่และไม่ถือ วัดได้ทางเดียวคือดูพฤติกรรมจริง
  - วิธีที่ใช้ได้: เปิดแถบค้นหาของ Safari (⌘F) แล้วกด Esc — กล่องเปิดอยู่ แถบค้นหาต้องยังอยู่ (เรากลืน) กล่องปิดแล้ว แถบค้นหาต้องหาย (เราคืนคีย์) ส่วน ⌘H เช็คด้วย `visible of process "Safari"` ว่าซ่อนได้จริง

## Phase 5.7: คีย์ลัดในแชท และตัวเลือกที่กดเลือกได้

- [x] ข้อความที่ขึ้นต้นด้วย `-` ต้องส่งได้ (bullet list, คำถามเกี่ยวกับ flag)
- [x] Shift+Enter ขึ้นบรรทัดใหม่ได้ ไม่ใช่แค่ Option+Enter
- [x] ลูกศรขึ้น/ลง เรียกข้อความที่เคยส่งในเซสชันนั้นกลับมาได้ เหมือน terminal
  - [x] ลงจนสุดแล้วได้ข้อความที่พิมพ์ค้างไว้คืน ไม่หาย
  - [x] ทำงานเฉพาะตอนกล่องพิมพ์เป็นบรรทัดเดียว — ถ้าหลายบรรทัด ลูกศรเป็นของการเลื่อน caret
- [x] Esc ปิดหน้าต่างแชท (ทำงานไม่ว่า focus อยู่ตรงไหน)
- [x] เวลา AI ถามให้เลือก ต้องขึ้นกล่องตัวเลือกที่กดลูกศรเลือกและ Enter ยืนยันได้ (คลิกได้ด้วย)
- [x] scroll ลงล่างสุดแล้วข้อความบรรทัดสุดท้ายต้องไม่ติดขอบจนดูเหมือนโดนตัด
- [x] code / JSON ในคำตอบต้องแสดงเป็นกล่อง code (monospace, รักษาการเยื้องบรรทัด, เลือกคัดลอกได้)
  - [x] บรรทัดยาวห้าม wrap — ให้ scroll แนวขวางเฉพาะกล่องนั้น เหมือนตาราง (การ wrap โค้ดใส่การขึ้นบรรทัดที่ไม่มีจริง อ่านแล้วไม่รู้ว่าต้องพิมพ์อะไร)
  - [x] ต้องแยก block ออกมาก่อนถึง `MessageMarkdown` เพราะ `.inlineOnlyPreservingWhitespace` กลืน fence แล้วยุบเนื้อในเป็นบรรทัดเดียว (เคยเห็น JSON โผล่มาเป็น `json { "iso": ... }`)
  - [x] หา code fence ก่อนหาตาราง — ในกล่องโค้ดมี `|` ได้ ถ้าหาตารางก่อนจะฉีกโค้ดออกเป็นตาราง

## Phase 6: External tools

- [x] MCP-based integrations such as calendar, Slack, email, and knowledge sources.
  - [x] ไม่ต้องเขียนโค้ดเพิ่ม — ทดสอบแล้วใช้ได้อยู่แล้ว (2026-07-28) เพราะแอปขับ Claude Code ของ user ซึ่งโหลด MCP server จาก config ของเขาเองตามที่ Phase 3 ตั้งใจ
  - [x] หลักฐาน: ถาม "ตอนนี้กี่โมงแล้ว" ลอยๆ ใน session ใหม่ โมเดลหา tool เจอเองด้วย ToolSearch แล้วเรียก `mcp__my-tools__get_time` สำเร็จ ไม่ต้องผ่านรอบขออนุมัติ และลิสต์ MCP server ที่เข้าถึงได้เองได้ครบ (ทั้ง server ราย project และ global เช่น Figma)
  - [x] อย่าเพิ่ม `mcp__*` ลง allowlist หรือเขียน MCP client เอง — allowlist ปัจจุบันไม่ได้บล็อก MCP
- [x] Be able to understand the web content through Chrome Claude plug in (suggest user that app can use this approach when user ask about app to understand the web contents).
  - [x] ทำแล้ว (2026-07-28) — ไม่ใช่แค่ "แนะนำ" อย่างเดียว แอปอ่านหน้าเว็บที่ต้อง login ได้จริง
  - [x] เหตุผล: `WebFetch` ยิงจาก process ของแอปเอง ไม่มี cookie/session — หน้า login จะคืนหน้า sign-in มาแทนเนื้อหา แล้วโมเดลรายงานหน้า sign-in ว่าเป็นเนื้อหาของหน้านั้น ส่วน extension ไม่ได้ fetch แต่อ่าน DOM ของ tab ใน Chrome ที่ user login ค้างอยู่ — ยืม session ที่เปิดอยู่ ไม่เคยเห็น password
  - [x] กลไก: Claude Code มี flag `--chrome` ต่อกับ Claude in Chrome extension ผ่าน MCP server ชื่อ `claude-in-chrome` (22 tools) — ในเมื่อแอปขับ Claude Code อยู่แล้ว จึงได้มาทั้งชุด
  - [x] พิสูจน์แล้วว่า `--chrome` ต่อติดใน non-interactive (`-p --output-format stream-json`) ด้วย: init event รายงาน `"claude-in-chrome": "connected"` ไม่มี first-run dialog มาค้าง (Claude Code 2.1.220)
  - [x] default = ปิด เปิดได้ที่ Settings → Browser และบอกใน chat เมื่อเปลี่ยน จำค่าข้ามการเปิดแอป
  - [x] pre-approve เฉพาะ tool ที่ "แค่ดู" (`read_page`, `get_page_text`, `find`, console/network, `tabs_context_mcp`) ส่วน navigate/click/type/upload/JavaScript ต้องขออนุมัติผ่าน try→refuse→ask→retry เดิม — tool ที่ไม่รู้จักถือเป็นต้องขออนุมัติไว้ก่อน
  - [x] ข้อควรระวัง: เนื้อหาเว็บเป็น untrusted input และตอนนี้อ่านจาก browser ที่ login อยู่ ระบบ prompt สั่งให้ถือข้อความในหน้าเว็บเป็นสิ่งที่ "รายงาน" ไม่ใช่ "คำสั่งให้ทำตาม"
  - [x] ขับจริงในแอปแล้ว (ไม่ใช่แค่ unit test): ถามลอยๆ **โดยไม่มี project** ก็เข้าเส้นทาง agent ได้ โมเดลเรียก browser tool เองโดยไม่มีการ์ดขออนุมัติ (เพราะเป็น tool อ่าน) และเมื่อสั่ง `navigate` ถูกปฏิเสธ ก็รายงานตรงๆ ว่า "ยังไม่ได้เปิดแท็บ ยังไม่ได้ปิดอะไร" ไม่แกล้งทำเป็นสำเร็จ
  - [x] การกระทำในเบราว์เซอร์ (scroll/คลิก/พิมพ์/เปิดหน้า) ขออนุมัติผ่าน try→refuse→ask→retry เดิม
    - [x] `scroll` = tool `computer` ซึ่งครอบคลิกและพิมพ์ด้วย — `--allowedTools` แยกระดับ argument ไม่ได้ การ์ดจึงต้องเขียนขอบเขตจริง ห้ามเขียนแคบแล้วให้กว้าง
    - [x] `ActionClass.browserAction` แยกจาก `.localWrite` — การ์ดเคยพาดหัวว่า "Send to Claude?" และ "Writes files in the project" ซึ่งผิดทั้งคู่สำหรับการคลิกในหน้าเว็บ
    - [x] `offerToWiden` ต้องไม่บังคับ project — งานเบราว์เซอร์ไม่สังกัด project เคยทำให้การ์ดไม่ขึ้นเงียบๆ และความสามารถนั้นเข้าไม่ถึงเลย
    - [x] ข้อความปฏิเสธของ browser tool คือ "requires permission" ไม่ตรงกับวลีของ Claude Code — `isPermissionRefusal` ต้องรู้จัก แต่เฉพาะกับ browser tool เท่านั้น (วลีนี้กว้างเกินไป)
  - [x] ชั้นอนุมัติมี **สองชั้น** และแอปคุมได้ชั้นเดียว: allowlist ของแอป กับ site permission ของ extension เอง (ข้อความ "Claude in Chrome requires permission") ชั้นหลังต้องกด allow ใน Chrome — ไม่เข้า `offerToWiden` ของแอป และไม่ควรพยายามทำให้เข้า
  - [x] extension เห็นเฉพาะแท็บที่ถูกแชร์เข้า session (กดไอคอน Claude ที่แท็บ) ไม่ใช่ทุกแท็บที่เปิดอยู่
  - [x] ต้องมี Claude in Chrome extension (≥1.0.36) และ login แบบ subscription — ถ้าใช้ API key Claude Code จะปิด Chrome integration เอง แม้ส่ง `--chrome` ไป (แอป strip `ANTHROPIC_API_KEY` อยู่แล้ว)

## Phase 6.1: ติดตามตามเวลาจริง (`/loop`)

- [x] แอปติดตามเรื่องที่ user สั่งไว้ตามเวลาจริงได้ — ทุก N นาที Secretary ถามตัวเองด้วยคำถามที่ค้างไว้ แล้วตอบลงในสายแชท (เกิดจากงานจริง: คุม workshop อยู่ พิมพ์ไม่ได้ ต้องรู้ว่าถึงหัวข้อไหน)
- [x] โมเดล **ไม่มีนาฬิกาและไม่ได้รันอยู่ระหว่างข้อความ** — prompt ของแต่ละรอบต้องบอกเวลาจริงไปด้วย ไม่งั้นมันเดาเวลาจากบริบทเก่า
- [x] เข้าได้สองทาง เข้าโค้ดชุดเดียวกัน
  - [x] `/loop 10m <จะรายงานอะไร>` · `/loop stop` · `/loop` = ดูสถานะ (`/track` เป็น alias) รับ `10`, `10m`, `10 min`, `10 นาที`, `1h`, `90s` เพราะพิมพ์มือเดียวตอนกำลังยุ่ง
  - [x] โมเดลตั้งเองตามบริบทด้วย block ```loop (`every: 10m` + บรรทัดถัดไปคือสิ่งที่จะรายงาน) — **ห้ามเดาจากร้อยแก้ว** เหตุผลเดียวกับ ```choices: โมเดลพูดว่า "จะคอยดูให้นะ" ตลอดเวลา ถ้าเดาจะได้ timer ที่ไม่มีใครสั่ง
- [x] กฎที่ห้ามหาย เพราะเป็นสิ่งที่ทำให้ "ของที่พูดเองได้" ยังปลอดภัย
  - [x] ประกาศในแชททุกครั้งที่เริ่ม พร้อมวิธีหยุด และมี badge ⏱ ที่ header กดหยุดได้ 1 คลิก — ข้อความประกาศ scroll หายไปได้ แต่คำตอบที่โผล่มาเองต้องมีคำอธิบายอยู่บนจอเสมอ
  - [x] ทุกรอบแทรก activity box บอกว่านี่คือ loop check **โดยไม่สนค่า toggle activity** เพราะนี่ไม่ใช่ขั้นตอนของงานที่ user สั่ง แต่เป็น "เหตุผลที่มีข้อความที่ไม่มีใครขอ"
  - [x] ขอบเขต: 1 นาที – 2 ชั่วโมง และหยุดเองเมื่อครบ 12 ชั่วโมง (`LoopSchedule`) ใช้กับทั้งคำสั่งของ user และของโมเดล — อย่างแย่สุดจึงเป็น timer ที่เห็นอยู่ มีขอบเขต และกดหยุดได้
  - [x] session-only ไม่เขียนลง disk — loop ผูกกับสิ่งที่เกิดขึ้นตอนนี้ ถ้ารอดข้ามการเปิดแอป มันจะตื่นมาถามเรื่องตารางที่จบไปแล้ว (เหตุผลเดียวกับ write/browser grant)
  - [x] ห้ามยิงทับ: `streamingTask` เป็น single-flight ถ้ายิงตอนกำลังตอบจะ cancel คำตอบที่ user กำลังอ่าน — ถ้าไม่ idle ให้เลื่อนไปรอบหน้า (`postponed`) และไม่นับว่าส่งแล้ว
  - [x] `.beginExecuting` ออกจาก `.idle` ไม่ได้ — tick ต้องส่ง `.userBeganInput` → `.beginInterpreting` เหมือนข้อความที่ user พิมพ์ และต้องเข้า `startChat` ตรงๆ ไม่ผ่าน intent classifier (คำของเราเองอาจถูกอ่านเป็นคำสั่งแล้วไปรัน tool ที่ไม่มีใครสั่ง)
  - [x] ตัวจับเวลาอยู่ใน `Secretary` ไม่ใช่ใน view — loop ต้องเดินต่อแม้ปิดหน้าต่างแชท เพราะคนที่สั่งกำลังมองห้องประชุม ไม่ได้มองจอ

## Phase 6.7: เพิ่ม Token Usage
- [x] สามารถ chat ถาม AI usage token ได้ — `/usage` (หรือ `/tokens`) ตอบเป็นตารางในสายแชท
- [x] มี windows ที่เปิดได้จาก menu icon แล้วแสดง usage token ได้ — เมนูแถบสถานะ → Token Usage (⌘U)
  - [x] **เปิดค้างไว้ได้** เป็นหน้าต่างของตัวเอง ไม่ใช่แผงในกล่องแชท — ปิดแชทแล้วตัวเลขต้องไม่หายไปด้วย และ observe `Secretary.sessionUsage` จึงอัปเดตสด เปิดทิ้งไว้ก่อนถามคำถามแรกก็เห็นตัวเลขไหลเข้ามาเอง
- [x] **แก้บั๊ก: อ่าน cache token ตกไปทั้งสองก้อน** — `result` event ส่ง token มา 4 ก้อน แต่โค้ดอ่านแค่ `input_tokens` กับ `output_tokens` เทิร์นที่วัดจริงได้ `in=2 out=5` ทั้งที่ traffic จริงคือ 11,768 cache write + 24,436 cache read รวม **36,211** — ไม่ใช่แค่ไม่ครบ แต่ผิดคนละ order of magnitude และตัวเลขนี้ถูกเขียนลง audit log มาตลอด
  - แยก 4 ก้อนไว้ ไม่รวมเป็นเลขเดียว เพราะราคาต่างกันมาก (cache read ถูก, cache write แพง) และเป็นตัวบอกว่าบทสนทนายาวขึ้นแล้วจ่ายอะไรอยู่
  - **context ใช้ค่าของเทิร์นล่าสุด ไม่ใช่ผลรวม** — คำถามคือ "ตอนนี้ context เต็มแค่ไหน" ถ้าบวกสะสมจะทะลุ 100% ทั้งที่ยังว่างอยู่เยอะ (`contextWindow` อ่านจาก `modelUsage` ซึ่ง key เป็นชื่อโมเดล ต้องขุด ไม่ใช่ path ตายตัว)
  - **ตัวเลขเงินต้องมีคำกำกับเสมอ** — Claude Code ส่ง `total_cost_usd` มาแม้ใช้ subscription ซึ่งไม่ได้ถูกตัดเงินต่อ token เลขดอลลาร์ลอยๆ ในแชทอ่านแล้วเข้าใจว่าโดนเรียกเก็บ มีเทสบังคับว่าสรุปทุกครั้งต้องมีคำอธิบายติดไปด้วย
- [x] **แสดงโควตาของ subscription ด้วย** (session / รายสัปดาห์ / รายโมเดล พร้อมเวลารีเซ็ต) — ตอนแรกสรุปผิดว่าทำไม่ได้ เพราะดูแค่ stream event, `--help` และไฟล์ใน `~/.claude` แล้วปิดประเด็น ความจริง `claude -p -- /usage` ตอบข้อมูลชุดเดียวกับแผง interactive ได้เลย จึงยังอยู่ในกติกา "ขับ Claude Code ของ user" ไม่ต้องแตะ credential ไม่ต้องยิง endpoint เอง
  - บทเรียน: ก่อนสรุปว่า CLI ทำอะไรไม่ได้ **ให้ลองสั่งจริงก่อน** อย่าอนุมานจาก help กับ output format
  - เป็น text ที่ออกแบบมาให้คนอ่านในเทอร์มินัล และเป็นของโปรแกรมอื่นที่เปลี่ยน format เมื่อไหร่ก็ได้ ตัว parser จึงต้อง **อ่านออกหรือไม่พูดอะไรเลย** ห้ามเดา — ตัวเลข % นี้คือตัวที่คนใช้ตัดสินใจว่าจะทำงานต่อไหม ค่าที่ผิดหรือค่าค้างที่ดูเหมือนสด แย่กว่าช่องว่าง
  - กับดักที่มีเทสคุมไว้: บรรทัด `83% of your usage was at >150k context` เป็นเปอร์เซ็นต์ของคนละเรื่อง ห้ามกลายเป็นแถบ limit
  - [x] **จัดหน้าตาให้เหมือนแผง Usage ของ Claude app** — "Plan usage limits" + ชื่อแพ็กเกจ / Current session / หัวข้อ "Weekly limits" → All models, Fable / "Last updated: … ⟳"
    - เวลารีเซ็ตที่ใกล้กว่า 24 ชม. แสดงเป็น "Resets in 18 min" และเดินถอยหลังเอง (view มี tick ทุก 30 วิ ส่วนตัวเลขจริง poll 2 นาที) ไกลกว่านั้นใช้คำของ CLI ตรงๆ เพราะมี timezone ติดมาแล้ว — CLI ไม่บอกปี ต้องเดาปีที่ทำให้วันอยู่ข้างหน้า ไม่งั้นรีเซ็ตเดือนมกราจะอ่านเป็นอดีต 11 เดือน
    - ชื่อแพ็กเกจมาจาก `claude auth status` ซึ่งตอบเป็น JSON (`subscriptionType`) — reply เดียวกันมี email กับ orgId ด้วย **หยิบมาเฉพาะ tier เท่านั้น**
  - [x] **เพิ่มบล็อก requests / sessions** ที่แอป Claude ไม่มี — "Last 24h · 510 req · 11 sessions" พร้อมบรรทัดที่บอกว่าอะไรกินโควตา (เช่น "83% of your usage was at >150k context") และคำกำกับว่านับเฉพาะ session บนเครื่องนี้ ไม่รวมเครื่องอื่นและ claude.ai
    - **ตัด `Top skills` / `Top plugins` ทิ้ง** — สองบรรทัดนั้นบอกว่าเจ้าของกำลังทำงานอะไรอยู่ ซึ่งเกินหน้าที่ของมาตรวัดโควตา มีเทสกันไว้ว่าชื่อ skill/plugin ต้องไม่โผล่
  - [ ] **ไม่ทำ: ส่วน Usage credits** (เจ้าของตัดสินใจไม่เอา) — เดิมเขียนว่า "ยังทำไม่ได้ ($ spent, monthly spend limit, current balance, promotional credit) และตัวคูณของแพ็กเกจ ("Max (5x)" ได้แค่ "Max") — ตรวจ `/usage`, `/cost`, `claude auth status` และ `--help` ครบแล้ว ไม่มีที่ไหนเลย
    - ทางเดียวที่เหลือคือยิง endpoint ของ Anthropic เองด้วย OAuth credential ของ user ซึ่งแปลว่าแอปต้องเริ่มอ่าน credential ที่ตั้งใจไม่แตะมาตลอด + เป็น endpoint ที่ไม่มีเอกสาร (พังเงียบได้) + เป็นข้อมูลการเงิน — ต้องให้เจ้าของอนุมัติก่อน ไม่ทำเอง
  - poll ทุก 2 นาที **เฉพาะตอนหน้าต่างเปิด** และมีปุ่ม refresh — spawn process ทิ้งไว้ตลอดเพื่อวาดแถบที่ไม่มีใครดู ไม่ใช่สิ่งที่ companion ควรทำ

## Phase 7 : Info-window
- [x] สามารถเอาข้อมูลไปเปิด window ลอยๆ ค้างไว้ได้ เช่น เมื่อถามข้อมูลอะไรบางอย่าง แล้วได้เป็นตาราง ก็บอก app ได้ว่า แสดงข้อมูลนั้นแยกออกจาก chat หรือแสดงข้อมูลนั้นแยก window ก็จะดึงข้อมูลที่คุยกัน (ในส่วนที่ระบุ) ออกไปเป็น window แยกได้
  - [x] ใช้ marker block ```window (`title:` + เนื้อหา) **ห้ามเดาจากร้อยแก้ว** เหตุผลเดียวกับ ```choices และ ```loop — โมเดลตอบเป็นตารางตลอดเวลา ถ้าเดาจะเปิดหน้าต่างที่ไม่มีใครสั่ง
  - [x] ต้องตัด block ทิ้งจากข้อความ ไม่งั้นเนื้อหาโผล่สองที่ (ในแชทและในหน้าต่าง)
  - [x] ใช้ตัว render เดียวกับแชท (`MarkdownBodyView` ที่แยกออกมา) ตารางที่ดึงออกไปจึงหน้าตาเหมือนตอนอยู่ในแชท
- [x] แสดงได้หลาย window — เรียงเหลื่อมกัน และ**จำกัดที่ 10 บาน** เกินแล้วตัวเก่าสุดหลุดก่อน กันโมเดลที่ยิง block รัวจนกลบจอ
- [x] ปิดได้, hide ได้ (กดปุ่มปิด คือปิดและลบ, กด Esc คือ hide)
  - [x] Esc ต้องแยกทางกับ Esc ที่ปิดแชท — ถ้า pane อยู่หน้าสุด Esc เก็บ pane ถ้าไม่ใช่ค่อยปิดแชท ตัดสินที่เดียวใน handler ของ hotkey
  - [x] และต้องจดทะเบียน Esc เมื่อ**มีอะไรให้เก็บ** ไม่ใช่เฉพาะตอนแชทเปิด ไม่งั้นปิดแชทแล้วเหลือแต่ pane จะกด Esc ไม่ได้
- [x] list ของ windows แสดงอยู่ในกลุ่มเมนู Windows ที่เปิดได้จาก menu icon
- [x] กดที่เมนูก็แสดง window นั้นขึ้นมา **และดึงมาไว้หน้าสุด** — pane ที่ถูก Esc ซ่อนไว้ยังอยู่ในเมนูเสมอ
  - เดิมทำเป็น submenu {Show, Delete} ต่อบาน เจ้าของให้เอาออก — แถวที่มีหน้าที่เดียวคือ "เปิดให้ดู" ไม่ควรมี submenu มาขวาง และ Delete ที่อยู่ห่างจาก Show หนึ่งพิกเซลคือการกดพลาดที่รอเกิด ลบได้จากปุ่มปิดของบานนั้นเองและจาก Clear All
- [x] มีเมนู Show All (อยู่ก่อน Clear All) เพื่อเรียกทุกบานกลับขึ้นมา
- [x] ในเมนู Windows มีเมนู Clear all (อยู่ล่างเสมอ) เพื่อ clear ทุก windows ที่มีอยู่
- [x] ขนาดของ window จะขยายตาม content ระดับนึง แต่ถ้าเนื้อหาเยอะเกินไป ก็ scroll ได้ (320–720 กว้าง, 180–640 สูง)
- [x] **บั๊กที่เจอตอนขับจริง: prompt ไปไม่ถึง backend ที่ใช้จริง** — `capabilityPrompt` ที่อธิบาย ```loop กับ ```window ใช้เฉพาะเส้นทาง API key ส่วน Claude Code ใช้ `agentPrompt` คนละตัวซึ่งไม่มีทั้งคู่ ขอให้ปักตารางแล้วโมเดลตอบว่า "no window tool is available to me in this session" ซึ่งถูกจากมุมของมัน — แปลว่า loop ที่โมเดลตั้งเองตาม Phase 6.1 ก็ไปไม่ถึงเช่นกัน มาตลอด
  - เทสใหม่ยิงหนึ่งเทิร์นบน backend ทั้งสองแบบ แล้วเช็คว่า fence ทุกตัวที่แอป parse ได้ ต้องปรากฏใน prompt ที่ส่งจริง — parser ถูกและเทสเขียวไม่ได้แปลว่าโมเดลรู้ว่ามีความสามารถนี้

## Phase 7.1: ลำดับปุ่มท้ายกล่องแชท

- [x] **ไม่ใช่แถวเดียว แต่เป็นสองกลุ่ม** — Projects อยู่ริมข้างหนึ่งตัวเดียว ส่วน Profile กับ Settings อยู่ติดกันที่อีกริมหนึ่ง มีความกว้างของหน้าต่างคั่นกลาง

  ```text
  | [Projects]                        [Profile] [Settings] |
  ```

  - รอบแรกผมทำเป็นแถวเดียวเรียงติดกันชิดซ้าย ซึ่งผิด เจ้าของวาดภาพให้ดูถึงเข้าใจ
- [x] ตอน mirror ต้องเป็น**ภาพสะท้อนของตัวเอง** กลับลำดับทั้งหมด**รวมถึงช่องว่างตรงกลาง** ไม่ใช่แค่สลับกลุ่ม — ถ้าสลับแค่กลุ่มแล้วคงลำดับในกลุ่มไว้ Profile จะไปอยู่ตำแหน่งที่ Settings เคยอยู่

  ```text
  | [Settings] [Profile]                        [Projects] |
  ```

  - ช่องว่างจึงเป็นสมาชิกของลำดับ (`FooterSlot.gap`) ไม่ใช่สิ่งที่ view เติมเอง เพราะมันคือสิ่งที่ทำให้เป็นสองกลุ่ม และต้องขยับตามเวลา mirror
  - กฎอยู่ใน `footerSlots` ที่ `SecretaryCore` มีเทสคุมทั้งสองด้าน ไม่ใช่ array ตายตัวในไฟล์ view
- [x] ตอนนี้ปลายแถวมีปุ่มทั้งสองข้าง กริป resize จึงหลบด้วยการย้ายปุ่มไม่ได้อีก ต้องเว้นที่ให้ที่มุมล่างที่กริปอยู่แทน

## Phase 8: เลือก Skills ที่ติดตั้งใน Claude Code ได้
- [x] เพิ่มปุ่ม Skills อยู่ระหว่าง Profile และ Settings
- [x] เมื่อกดปุ่ม Skills App จะแสดงหน้าจอเหมือน settings
- [x] ในหน้าจอจะมี list ของ Skills ที่มี — สแกน `~/.claude/skills`, `.claude/skills` ของทุก project ที่ลงทะเบียนไว้โดยตรง, และ skill ที่มากับ plugin ที่ enable อยู่ (`SkillDiscovery`) เพราะ `claude` CLI ไม่มีคำสั่ง list skills จริง (`claude plugin list` มีแต่ plugin ที่ลงทะเบียนผ่าน marketplace เท่านั้น ไม่เห็น skill folder เปล่าๆ หรือ skill ข้างในแต่ละ plugin)
  - plugin skill อ่านจาก `enabledPlugins` ใน `~/.claude/settings.json` (ไม่ใช้ plugin ที่ถูก disable แม้จะยังมีไฟล์ cache ค้างอยู่) แสดงชื่อแบบ namespaced เช่น `superpowers:brainstorming` กันชนกับ skill จาก plugin อื่น
- [x] สามารถ check เพื่อเลือกใช้ Skills ใน session นั้นได้ — session-only เหมือน `activeLoop` (ไม่เขียนลง disk) ผลของการเลือกคือ soft hint ใน system prompt ("only use these skills…") ไม่ใช่ hard limit เพราะไม่มี flag ของ CLI ที่ enforce ระดับ skill ได้จริง (มีแต่ `claude plugin enable/disable` ซึ่ง global/persist ข้าม session ไม่ตรงกับที่ต้องการ)

## Phase 9.1: Monitor files and folders changed
- [x] App สามารถ monitor ความเปลี่ยนแปลงของ folder ได้เมื่อสั่งให้ทำ — `/watch <path>`
  (`.` คือโฟลเดอร์โปรเจกต์) ดูทุก 4 วินาทีด้วยการเทียบ snapshot บอกว่าอะไร เพิ่ม/หาย/แก้
  ต้องสั่งเท่านั้น ไม่มีการ watch เองอัตโนมัติ
  - มีเพดานโดยตั้งใจ: 500 entry / ลึก 4 ชั้น / ข้าม `.git` `.build` `node_modules` ฯลฯ
    เพราะโปรเจกต์ที่ลงทะเบียนมักเป็นทั้งรีโป **และจะบอกเมื่อชนเพดาน** ("the first 500 files")
    เพราะ "ดูโฟลเดอร์นี้" กับ "ดู 500 ไฟล์แรกของโฟลเดอร์นี้" เป็นคำสัญญาคนละอัน
- [x] App สามารถ monitor ความเปลี่ยนแปลงเนื้อหาของ file ได้เมื่อสั่งให้ทำ — ไฟล์เดี่ยวใช้
  hash ของ *เนื้อไฟล์* ไม่ใช่ size+mtime (ซึ่งใช้กับ entry ในโฟลเดอร์) เพราะกด save ทับ
  โดยไม่แก้อะไรก็ขยับ mtime ถ้ารายงานก็จะเป็นการร้องหมาป่าทุกครั้งที่ ⌘S
- [x] App จะยกเลิกการ monitr เมื่อสั่ง — `/watch stop` หรือกด × ที่ badge รูปตาบน header
  (badge มีไว้เพราะของที่พูดเองต้องมองเห็นได้ตลอดว่าเปิดอยู่ และปิดได้ในคลิกเดียว)
- [x] เฝ้าได้หลายที่พร้อมกัน (สูงสุด 5) — โฟลเดอร์ดูไฟล์เข้าออก + ไฟล์ดูเนื้อหา ทำพร้อมกันได้
  แต่ละอันรายงานแยกข้อความ (เป็นคนละคำถามที่ถามคนละเวลา "3 changes" คร่อมสองที่จะไม่ตอบอะไรเลย)
  `/watch stop <path>` หยุดทีละอัน, `/watch stop` หยุดหมด, badge บอกจำนวน
  - เพดาน 5 เพราะแต่ละอันคือการเดินไฟล์ทุก 4 วินาที ชนแล้วปฏิเสธอันใหม่ ไม่ไปหยุดอันที่วิ่งอยู่
- [x] ไม่ต้องพิมพ์คำสั่งเอง — ขอด้วยภาษาคนได้ AI จะสั่งให้ ผ่าน block ```watch เหมือนที่
  ```loop ทำอยู่ (ห้ามเดาจากร้อยแก้ว: "เดี๋ยวดูให้นะคะ" ต้องไม่กลายเป็น background job)
  ตัวกันเดิมอยู่ครบ — path ต้องอยู่ในโปรเจกต์ที่อนุมัติ, ครั้งแรกในโปรเจกต์ยังเจอการ์ด,
  ประกาศตอนเริ่ม, กดปิดได้ที่ badge

## Phase 9.2: Follow instruction from file
- [x] App สามารถทำงานได้ตาม flow หรือ instruction ที่ถูกเขียนใน file — `/run <file>` อ่านไฟล์
  แล้วเดินทีละ step โดยแต่ละ step เป็นหนึ่งเทิร์นของ Claude Code ตามปกติ จึงยังผ่าน
  permission card เดิมทุกครั้งที่จะลงมือทำอะไร
- [x] user จะระบุว่า ไฟล์ไหนเป็น instruction — ต้องพิมพ์ path เอง ไม่มีการเดาชื่อไฟล์
  และการอ่านถูกจัดเป็น `.externalNetwork` (เหมือน `FileUnderstanding`) จึงถามทุกครั้ง
  เพราะเนื้อไฟล์ออกจากเครื่อง
- [x] app สามารถเข้าใจรูปแบบของ file ทั้งแบบภาาาธรรมชาติ, step-by-step, diagram (แบบ markdown หรือที่ AI เข้าใจ), หรือ LangGraph
  — ไม่ได้เขียน parser ของแต่ละรูปแบบ (สี่ parser ที่ยังไงก็ไม่ครอบคลุมแบบที่ห้า) แต่ให้โมเดล
  อ่านแล้วตอบกลับเป็น block ```plan อย่างเดียว ตัวแอปเห็นแค่รูปแบบเดียว
- [x] App จะยืนยัน step กับ user ก่อนเริ่มงานเสมอ — `InstructionPlanCard` โชว์ทุก step ตามลำดับ
  ไม่ย่อ ไม่เริ่มจนกว่าจะกด Start
- [x] เมื่อเริ่มงานแล้ว ถ้า file มีการเปลี่ยนแปลง app จะไม่ทำจนกว่าจะหยุดแล้วทำใหม่ — pin ด้วย
  SHA-256 ของเนื้อไฟล์ (ไม่ใช่ mtime) เทียบก่อนทุก step ไม่ตรงคือหยุดแล้วบอกเหตุผล
- [x] ถ้าขั้นตอนใน file เดิม มีการเปลี่ยนแปลงจากรอบที่แล้วใน session นั้น ให้ถาม confirm ความเปลี่ยนแปลงก่อนเริ่ม
  — `InstructionMemory` (session-only) จำ fingerprint ต่อ path การ์ดจะขึ้นว่า "changed since
  you last ran it" รอบแรกไม่ขึ้น เพราะยังไม่มีอะไรให้เปลี่ยน
- [x] App จะระวังเรื่อง Prompt injection ถ้าเป็นคำสั่งที่อันตรายหรือมีแนวโน้มประสงค์ร้าย ต้องถาม user ก่อนเสมอก่อรเริ่มทำงาน
  — สแกนเองด้วย `instructionRisks` (deterministic) ไม่ฝากให้โมเดลตัดสินไฟล์ที่กำลังหลอกโมเดลอยู่
  เจอแล้ว **escalate ไม่ block**: ขึ้นคำเตือนพร้อมคำที่ทำให้ติด แล้วต้องติ๊ก checkbox ก่อน Start
- [x] เมื่อผ่านขั้นตอนการตรวจสอบ App จะเริ่มทำงานจนเสร็จ หรือไม่ก็เมื่อ user สั่งยกเลิก — badge
  ที่ header บอก step ที่กำลังทำและกดหยุดได้ หรือ `/run stop`
- [x] ขอด้วยภาษาคนได้เช่นกัน ผ่าน block ```run — ปลอดภัยเพราะ "ขอ" ไม่ใช่ "ทำ": ไปได้ไกลสุด
  แค่การ์ดยืนยัน step ซึ่งเป็นด่านเดียวกับที่พิมพ์ `/run` เอง

## Phase 10: Chat History
 - [x] เพิ่มเมนู Chat History ใน Menu icon, อยู่ระหว่างเมนู New Conversation และ Hide Character
 - [x] มีเส้นคั่น group ก่อนเมนู Hide Character
 - [x] เมื่อเริ่ม Chat, app จะเพิ่ม history ในเมนูนั้น (app ตั้งชื่อให้ด้วยจาก context) และ app จะคอยจำ context ที่คุยกันไว้ด้วย
 - [x] เมื่อ new chat, app จะเก็บ chat เดิมลง history แล้วสร้าง chat sessionใหม่แทนใน chat window เดิม และ clear content ใน window เดิมด้วย
  - [x] เมื่อเลือก chat ใน history, chat window จะแสดง content ของ history นั้น และ app จะดึง context เดิมและคุยต่อได้เลย
  - [x] Chat History มีสูงสุด 10 รายการ และมี menu Clear all

ที่ตัดสินใจไว้ระหว่างทำ และคนที่มาต่อควรรู้ (เสร็จ 2026-08-06, v0.10.174):
- **ชื่อบทสนทนามาจากข้อความแรกของ user ตรงๆ ไม่ได้ให้โมเดลตั้ง** — ให้โมเดลตั้งคือเพิ่ม
  จุดล้มเหลว + กิน turn + ช้า ในจังหวะที่คนกำลังจะเริ่มเรื่องใหม่ ทั้งหมดนี้เพื่อ label ในเมนู
- **"คุยต่อได้เลย" คือการเก็บ session id ของ Claude Code ไม่ใช่การยัดประวัติกลับเข้าโมเดล** —
  แอปส่งเฉพาะข้อความล่าสุดแล้วใช้ `--resume` อยู่แล้ว
- **วัดจริงกับ Claude Code 2.1.220: `--resume` ข้าม working directory ได้ และข้ามโปรเซสได้**
  ซึ่งขัดกับที่โค้ดเดิมเชื่อ `prepare()` เคยทิ้ง session ทุกครั้งที่ย้ายไดเรกทอรี ทำให้ resume
  พังเงียบๆ ตอน resolve project ในเทิร์นแรก **และทำให้เสีย context ของข้อความแรกทุกครั้ง
  ที่ approve project มาตลอด** เอาออกแล้ว — ถ้า session ตายจริงจะกลายเป็น `.staleSession`
  ซึ่งเริ่มใหม่ให้พร้อมบอก จึงปลอดภัยกว่าการทิ้งไว้ก่อนโดยไม่ลอง
- **จอกับความจำแยกกันพังได้** ถ้า Claude Code ลบ session ไปแล้ว บทเก่าจะอยู่ครบบนจอแต่
  โมเดลไม่เห็นเลย → เพิ่ม event `sessionLost` ให้พูดออกมา ห้ามปล่อยเงียบเด็ดขาด
- **คำสั่ง `/…` ไม่ใช่บทสนทนา** — เคยได้แถวชื่อ "/history 1" เพราะการเปิดบทเก่าไปเก็บคำสั่ง
  ที่ใช้เปิดเอาไว้เป็นบทใหม่
- **default ของ store เป็น in-memory มีแต่ AppDelegate ที่ชี้ไปไฟล์จริง** — ตอนแรก default
  เป็นไฟล์จริง รันเทสรอบเดียวเขียนบทสนทนาปลอม 9 อันลงไฟล์ของเจ้าของ

## Phase 11: ทำงานกับ web app ใดๆ แทน user
- [x] user สามารถบอก app ได้ ว่าให้ทำงานกับ web ใดๆ ผ่าน chat ได้ (ผ่าน Chrome extension)
- [x] user จะส่ง url ให้ทาง chat แล้ว app จะเริ่มขอ permission — ลิงก์ในข้อความเปิดการ์ดของตัวเอง
  ก่อนอะไรทั้งนั้น กด Go ahead แล้วจึงได้สิทธิ์ "ทำงานในเว็บนี้" ตลอดบทสนทนานี้ (+ ต่อ Chrome ให้
  ถ้ายังปิดอยู่ + อนุญาต `navigate`) แล้ว app จะรันข้อความเดิมต่อให้เอง ไม่ต้องพิมพ์ซ้ำ
- [x] app ตรวจสอบว่า web app นั้นคืออะไร และทำความเข้าใจ context ของ user — prompt สั่งให้ดูก่อน
  ลงมือ แล้วรายงานว่านี่คือแอปอะไร บัญชีไหน อยู่หน้าไหน
- [x] user จะส่งข้อมูลให้ app ได้ ในกรณีที่ app ต้อง submit ข้อมูลลง web (data entry) โดยข้อมูลที่รองรับคือ
  - [x] text ใน chat เป็นภาษาธรรมชาติ, AI จะตีความเพื่อ map เข้ากับ web
  - [x] text ใน chat ด้วย markdown, หรือ CSV (เมื่อ paste ลงไปใน chat, content ใน chat window
    จะแสดงเป็น read format) — pipe table มีอยู่แล้ว, เพิ่ม CSV/TSV/semicolon ใน `DelimitedTableParser`
  - [x] upload file ได้ ด้วยการ drag/drop ใน text box หรือบอก AI ว่าจะ upload แล้ว AI จะส่ง
    open file button ให้ user กดเปิด open file dialog — ปุ่มมาจาก block ```attach
  - [x] รองรับ .md, .csv, JSON, key:value, รูปภาพที่เป็น text และ AI แกะ content ออกมา
- [x] AI จะตีความและ confirm user ถ้าเนื้อหากำกวม — ใช้ block ```choices ที่มีอยู่ และ prompt สั่งให้
  โชว์ mapping ก่อน submit
- [ ] test scenario: ใช้ chrome เปิด gmail ที่ให้ user login รอไว้ก่อน แล้วส่ง test email ไปที่ olarn.u@odds.team
  — **ยังไม่ได้ทำ และคนที่มาต่อก็ทำแทนไม่ได้**: คำสั่งที่ยิงคีย์เข้าแอปเพื่อให้ส่งอีเมลจริง
    ถูก classifier ของ Claude Code บล็อก (ถูกแล้ว — เป็นการกระทำที่มีผลออกไปข้างนอก)
    ส่วนที่ขับได้ทำครบแล้ว: เปิด Gmail ของเจ้าของผ่าน Chrome, อ่าน inbox จริง, บอกได้ว่าเป็นบัญชีไหน
    เจ้าของพิมพ์ประโยคเดียวในแชทก็จบ — ที่เหลือคือการ์ด browser action เดิมที่มีอยู่แล้ว

ที่ตัดสินใจไว้ระหว่างทำ และคนที่มาต่อควรรู้ (เสร็จ 2026-08-06, v0.10.179):
- **สิทธิ์ผูกกับ "เว็บ" ไม่ใช่ "หน้า"** — ถ้าถามทุก URL ของบอร์ดเดียวกัน การ์ดจะกลายเป็นของที่กดผ่าน
  โดยไม่อ่าน ซึ่งแย่กว่าไม่ถาม subdomain นับเป็นคนละเว็บ (help page ไม่ลามไป admin console)
- **ตัวจับ URL ตั้งใจให้โง่** — ต้องมี `http(s)://` หรือขึ้นต้น `www.` เท่านั้น เพราะถ้ารับ bare domain
  คำว่า `config.json` / `node.js` / `v2.1` จะเปิดการ์ดขออนุญาตกลางบทสนทนาเรื่องไฟล์
- **ไฟล์แนบถูก "คัดลอก" ไม่ใช่ "ชี้"** — ถ้าชี้ไปที่ไฟล์เดิม ต้องเปิดโฟลเดอร์ต้นทางให้ backend
  ลากไฟล์จาก Desktop ทีเดียว = ทั้ง Desktop อ่านได้ทั้ง session สำเนายังเป็น snapshot ด้วย
  (ไฟล์ที่ถูกแก้กลางเทิร์นไม่ทำให้คำตอบเพี้ยน) ล้างทิ้งทุกครั้งที่ขึ้นบทสนทนาใหม่
- **note ที่บอก path ของไฟล์แนบ ต้องต่อ *หลัง* classifier** — ตอนแรกต่อเข้าไปในข้อความก่อน
  classify ทำให้ "read this" กลายเป็นคำสั่งเปิดโปรเจกต์ชื่อ "what they asked:" (เทสจับได้)
- **queue ต้องพาไฟล์ไปด้วย** — ไฟล์ถูกถอดออกจากรายการตอนกด Return ถ้า queue เก็บแต่ข้อความ
  ข้อความที่ได้คิวจะพูดถึงไฟล์ที่ไม่มีใครส่ง `QueuedMessage` จึงถือทั้งคู่
- **CSV ต้องดูเหมือนข้อมูลไม่ใช่ประโยค** — แค่นับจำนวนช่องเท่ากันไม่พอ "ไปตลาด, แล้วกลับบ้าน"
  สองบรรทัดกลายเป็นตารางสองคอลัมน์ เพิ่มกติกา: ช่องสุดท้ายห้ามจบด้วย . ! ? และแต่ละช่องสั้น
  ผลข้างเคียงที่ยอมรับ: คอลัมน์โน้ตยาวๆ จะไม่ถูกจัดเป็นตาราง ซึ่งพลาดในทางที่ปลอดภัยกว่า
- **ชนิดไฟล์: ชื่อไฟล์ตัดสิน "เรียกว่าอะไร" ส่วนไบต์ตัดสิน "รับหรือไม่รับ"** — ลิสต์นามสกุลไม่มีวันครบ
  (v0.10.181) ตอนนี้ .pdf และ source code มีชนิดของตัวเอง ส่วนนามสกุลที่ไม่รู้จักจะถูกอ่าน 4KB แรก
  ถ้าเป็น UTF-8 และไม่มี NUL ก็รับเป็น text ถ้าไม่ใช่ก็ปฏิเสธพร้อมบอกเหตุผล
- **คอมมาในเลขหลักพันไม่ใช่ตัวคั่นคอลัมน์** (v0.10.182) — เจอตอนขับจริง: คำตอบ "total 1,250 THB"
  สองบรรทัดกลายเป็นตารางสองคอลัมน์ และเลขถูกตัดครึ่ง CSV จริงที่ตั้งใจให้คั่นตรงนั้นต้องใส่ quote อยู่แล้ว
- **tab group ของ Chrome ผูกกับ session ของ Claude Code ไม่ใช่กับตัวแอป** (v0.10.183) —
  วัดกับ 2.1.220: session เดิม (`--resume`) = `tabGroupId` เดิม, session ใหม่ = group ใหม่ + หน้าต่างใหม่
  และไม่มี tool ไหนของ extension ที่ระบุ group ได้เลย (`tabs_context_mcp` เห็นเฉพาะ group ของ session ตัวเอง)
  - ที่เป็นบั๊กของเราคือ `setBrowserEnabled` ทิ้ง session ทุกครั้งที่เปิด browser กลางบทสนทนา
    บนความเชื่อว่า resume แล้วจะไม่ได้ tool ชุดใหม่ — **วัดแล้วไม่จริง** session ที่เริ่มโดยไม่มี
    `--chrome` พอ resume พร้อม `--chrome` ก็ได้ tool ครบ (เป็นความเชื่อผิดแบบเดียวกับ `prepare()` ใน Phase 10)
  - prompt สั่งเพิ่มให้ทำงานใน group ที่มีอยู่ ใช้แท็บเดิม และห้ามปิดแท็บสุดท้าย เพราะ Chrome
    จะลบ group ทิ้งแล้วครั้งหน้าจะเปิดหน้าต่างใหม่ — คำอธิบายของ tool เองสั่งตรงข้าม
    ("สร้างแท็บใหม่ทุกบทสนทนา แล้วเก็บกวาดให้เรียบร้อย") จึงต้องเขียนทับให้ชัด
  - **ข้อจำกัดที่เหลือ**: บทสนทนาใหม่ = session ใหม่ = group ใหม่ แก้จากฝั่งเราไม่ได้
- **ไม่เขียน parser ของ .csv/.json/key-value เอง** — ให้โมเดลเปิดไฟล์ด้วยเครื่องมือของมันเอง
  เขียนเองคือได้ parser สามตัวที่แย่กว่าของเดิม แล้วรูปแบบที่สี่ก็ยังมาอยู่ดี

## Phase 12: ปรับ setting ให้อยู่ถูกที่ถูกทาง
- [x] ย้าย App size จาก Profile -> Settings
- [x] ย้าย Model, Effort จาก Settings -> Profile
- [x] ย้าย Browser จาก Settings -> Project

เกณฑ์ที่ใช้ตัดสินว่าอะไรอยู่แผงไหน (เสร็จ 2026-08-12, v0.10.202) — เขียนไว้เพราะแถวถัดไปที่
เพิ่มเข้ามาต้องตอบคำถามเดียวกันนี้ ไม่ใช่ไปลงแผงที่ว่างที่สุด:

- **Settings = แอปหน้าตายังไง** ตอนนี้มีสี่แถวและไม่มีอะไรอื่น: Theme, App size, Text size,
  Chat height
- **Profile = ใครเป็นคนตอบ** รวมถึง Model กับ Effort ซึ่งไม่ใช่ field ของ profile ที่เก็บลงดิสก์
  จึงมีผลทันทีที่กด ต่างจากแถวข้างบนที่รอ Save
- **Projects = ที่ที่ยอมให้เข้าไปอ่าน** browser grant เป็นของชนิดเดียวกับโฟลเดอร์โปรเจกต์
  ไม่ใช่ของชนิดเดียวกับขนาดตัวอักษร

สองอย่างเปลี่ยนรูปตอนย้าย เพราะย้ายไปแล้วไปอยู่ข้างของที่ขัดกันเอง:

- **Model กับ Effort เคยโชว์แต่ชื่อ ไม่โชว์ค่า** — `Menu` ที่ label ประกอบจากหลาย view จะ
  render เหลือแค่ chevron ในหน้าต่างนี้ แถว Browser เลี่ยงมาก่อนแล้วด้วยการเอา title ออกมาไว้
  นอกเมนู พอย้ายเข้า Grid ของ Profile ก็ได้รูปนั้นฟรี เพราะ label column ถือ title ส่วนเมนูถือ
  แต่ค่า (`settingControl`) ตัว `settingLabel` เดิมที่ผลิต label เปล่าถูกลบทิ้ง
- **App size เคยบอกค่าปัจจุบันด้วยการ disable ปุ่มนั้น ส่วน Theme ทาสี accent** — พอมาอยู่
  ติดกันคือสองแถวที่พูดว่า "อันนี้คืออันที่ใช้อยู่" คนละภาษา และภาษาหนึ่งแยกไม่ออกจาก
  "อันนี้กดไม่ได้" ตอนนี้ทั้งคู่ผ่าน `choiceRow` ตัวเดียวกัน

ขับจริงที่ v0.10.202 (build 95ca645):

- เปิดครบสามแผง และสลับแผงขณะเปิดอยู่
- **Profile ที่ 10pt (พื้น) และ 24pt (เพดาน)** ซึ่งเป็นสองสถานะที่ charter ระบุไว้ ที่ 10pt พอดี
  ทั้งแผงเห็นตั้งแต่หัวข้อถึงปุ่ม Save ส่วนที่ 24pt ยาวเกิน cap แล้ว scroll ในตัวเองจนถึง Save
  ตามที่ออกแบบไว้ — แผงนี้โตขึ้นจากรอบนี้ (เพิ่มสองแถวกับ hint สามบรรทัด) จึงต้องดูทั้งสองปลาย
- เปิดเมนู Model แล้วเลือก Claude Opus 5 จริง — แถวเปลี่ยนค่า วงแหวนประ inherited หายไป และ
  บทสนทนาประกาศให้เหมือนตอนพิมพ์ `/model` แล้วกดกลับเป็น default
- **กดทั้งสองแถวที่ผ่าน `choiceRow`** ไม่ใช่แถวเดียว เพราะทั้งคู่ถูกเขียนใหม่พร้อมกัน: App size L
  แล้วหน้าต่างตัวละครโตจาก 128×149 เป็น 167×194 จริง, Theme Light แล้ว palette พลิกทั้งแอป
  accent ย้ายไปปุ่ม Light และคำอธิบายใต้แถวเปลี่ยนเป็น "Always light"

## Phase 13: Multi-AI-Secretaty
- [x] ทำความเข้าใจโครงสร้างใหม่ ใน file menu.pdf
- [x] app สามารถ สร้าง character ได้มากกว่า 1 ตัว โดยจะมีเมนู New Character แล้ว app จะแสดง window
  ให้ create character ใหม่ โดยใช้ configuration จาก Profile เดิม
- [x] character แต่ละตัว จะแยก session กัน ตามที่ออกแบบไว้ใน menu.pdf
- [x] ปรับเมนูใน menu bar ตาม menu.pdf

**Character = Profile** (เจ้าของตัดสิน 2026-08-13) ทุก profile ใน `ProfileLibrary` คือตัวละครที่อยู่บนจอ
ไม่มี type ใหม่ และ New Character คือ add profile ที่ clone มาจากตัวที่กดอยู่ ทำเป็น 5 ก้อนที่ ship
ทีละก้อน (v0.10.203–208) เพราะก้อนแรกเป็น refactor ล้วนที่ต้องพิสูจน์ด้วยเทสเดิมที่ไม่ถูกแก้

สิ่งที่คนมาต่อควรรู้:

- **`CharacterInstance` คือหน่วยที่คูณได้** — ของที่เป็นของตัวละคร (state machine, Secretary,
  สองหน้าต่าง, bubble layout, backend, pinned panes, visibility) เคยเป็น property เดี่ยวๆ บน
  `AppDelegate` ทั้งหมด ที่เหลืออยู่ข้างนอกคือของแอปจริงๆ: theme, roster, registry, status bar
  และ Esc ซึ่งระบบให้ได้ครั้งเดียว ตัวละครแค่ "รายงานว่าคำตอบเปลี่ยน" ไม่ได้ตัดสินเอง
- **roster เป็น reconcile ไม่ใช่ add/remove** — `reconcileCharacters()` ทำให้คนบนจอตรงกับ profile
  ที่มี ทั้ง launch, add, delete เดินทางเดียวกัน ไม่มีที่สองที่จะเถียงกันว่าใครอยู่บนจอ
- **`ChatBackend` ทำสองงานปนกัน** — หา Claude Code (ช้า ต้องแชร์ เพราะ fallback เปิด login shell)
  กับถือ session (ต้องแยก) แยกเป็น `ClaudeCodeDetector` + backend ต่อตัว ถ้าไม่แยก สองตัวละคร
  จะตอบลงเธรดเดียวกัน
- **เมนูเป็น value ไม่ใช่โค้ดสร้าง `NSMenuItem`** — `statusBarMenu(...)` ใน `SecretaryCore`
  มีเทสยืนยันทีละแถว ทุก action พก id ของตัวละครไปด้วย เพราะ "Clear All" ต้องบอกได้ว่าของใคร
- **`activeID` เหลืองานเล็กลง** — ไม่ได้แปลว่า "ตัวที่เห็น" อีกแล้ว แต่แปลว่า "ตัวที่ถูก clone
  ตอนสร้างใหม่" และ "ตัวที่ fallback ไป"
- **`onActiveChange` ต้องแตกเป็นสอง** — ของเดิมยิงเฉพาะ profile ที่ active ซึ่งถูกตอนมี profile
  เดียวบนจอ และผิดทันทีที่ทุก profile มี prompt ของตัวเอง (แก้ Kai ตอน Miku active แล้วไม่ถึงใคร)

ขับจริงที่ v0.10.208 (build 5631b03): สามตัวละครขึ้นพร้อมกันห่างกัน 179pt (= 128×1.4) แต่ละตัว
รูปและชื่อของตัวเอง, เปิดสองแชทพร้อมกันแยกกันสมบูรณ์, New Character สร้าง "Miku 2" ที่สืบทอด
อายุ/บุคลิกแต่ Picture: None, กด Delete แล้วทั้งตัวละครและหน้าต่างหายไปพร้อมกัน, migration ย้าย
`conversations.json` 39,877 ไบต์ไปเป็นไฟล์ต่อตัวละครแล้วประวัติยังอ่านเจอ

**เรื่องที่เจอตอนขับ ยังไม่ได้แก้:** ตัวละครยืนห่างกัน 179pt แต่บับเบิลกว้าง 423pt **แชทจึงทับกัน**
คลิกในเขตทับจะตกไปที่หน้าต่างบนสุด ไม่ใช่ตัวที่เล็งไว้ — เกี่ยวโดยตรงกับ Phase 15 ที่ออกแบบให้
bubble ซ้อนเหลื่อมกันเป็นชั้น

## Phase 13-1: Dedicate Project
- [x] แยก Project ตาม character แต่ user สามารถ set project เดียวกันได้เอง
- [x] แยก Skills ที่จะถูกเลือกให้ใช้ ตามแต่ละ character
- [x] แยก Settings ตามแต่ละ character

ship ทีละข้อ (v0.13.214–216) เพราะข้อ 2 ตอบไม่ได้จนกว่าข้อ 1 จะเสร็จ — รายการ skills
คำนวณจาก `registry.projects` ตัวเดียวกัน

สิ่งที่คนมาต่อควรรู้:

- **registry ไม่ใช่รายการโฟลเดอร์ แต่คือ allowlist** — `Project` พก `allowedTools` มาด้วย
  ไฟล์เดียวจึงแปลว่าอนุมัติ Claude Code ให้ตัวหนึ่งคืออนุมัติให้ทุกตัว เกณฑ์ตอนตรวจข้อ 1
  จึงเป็น "เพิ่มให้ A แล้ว B ต้องเข้าไม่ได้" ไม่ใช่แค่ "แผงของ B ไม่ขึ้น"
- **migration ใช้ฟังก์ชันเดียวกับ conversations** — `conversationFileMigration` ย้ายลงไปอยู่
  `ProjectRegistry` เป็น `perCharacterFileMigration` แล้วชื่อเดิมเหลือเป็น typealias
  หลักฐานว่าเป็น refactor: `ConversationFileMigrationTests` ผ่านโดยไม่ถูกแก้ ตัวที่ถูกสร้างก่อน
  รับ `projects.json` ไป ที่เหลือเริ่มจากศูนย์ — ตั้งใจ ไม่ใช่ของตกหล่น และเป็น rename
  ไม่ใช่ copy ตัวที่สองจึงหาไม่เจอ
- **Skills แยกอยู่แล้วตั้งแต่ก่อนเริ่ม** — `selectedSkills` อยู่บน `Secretary` ซึ่งมีตัวละครละตัว
  ขับยืนยันแล้ว (ติ๊ก grilling ให้ Ditto แล้วของอาเนียยังว่าง) ที่แชร์กันคือ *รายการ* ซึ่ง
  ตามข้อ 1 ไปเอง ของจริงที่พังคือรายการไม่เคย rescan: `projectsDidChange` re-scope workspace
  แต่ไม่แตะ skills เลย และออกทาง guard ของ provider ก่อนด้วย เครื่องที่ไม่มี Claude Code
  จึงไม่ขยับเลย — ตอนนี้ scan ก่อน guard และตอนลบ project ก็เรียกด้วย ซึ่งเป็นครึ่งที่สำคัญกว่า
  เพราะ session ที่วิ่งอยู่ถือ working directory เดิมไว้จนกว่าจะมีใคร re-scope
- **ทั้งเพิ่มและลบ project จะถามคำถามล่าสุดซ้ำ** ประกาศใน activity box ว่า "Workspace changed
  — asking again" ไม่ได้ทำเงียบๆ ของเดิมมีเฉพาะตอนเพิ่ม ตอนลบเป็นของใหม่รอบนี้ ขับยืนยันทั้งคู่แล้ว
- **สิ่งที่ยังไม่แก้ และไม่ได้เกิดจากรอบนี้: `selectedSkills` ไม่ถูกบันทึกลงดิสก์** เป็น session-scoped
  มาแต่เดิม (เขียนไว้ที่ `InstructionFile.swift`) ที่ติ๊กไว้จึงหายทุกครั้งที่ปิดแอป การแยกต่อ
  character เป็นจริงแล้ว แต่ "ติ๊กไว้แล้วลืม" น่าจะเป็นเรื่องถัดไปที่สะดุดตา
- **Settings แยกทั้งสี่ค่า รวม Theme** (เจ้าของตัดสิน 2026-08-13) หน้าต่างที่เป็นของแอป
  (Token Usage, About) ใส่ชุดของตัวที่เพิ่งใช้ล่าสุด `focused` จึงไม่ใช่ `characters.first`
  อีกต่อไป
- **ไม่มีการ rename บนดิสก์** — key ของเธอคือ `appearance.<uuid>.*` และค่าที่เธอไม่เคยแตะ
  จะ fallback ไปอ่าน key เดิมของแอป **ทีละค่า ไม่ใช่ทั้งชุด** launch แรกหลังรอบนี้ทุกตัวจึง
  หน้าตาเหมือนเมื่อวาน ส่วน save เขียนเฉพาะ key ของเธอ เพราะเขียนทับ key กลางจะลากคน
  ที่ยังไม่ได้เลือกไปด้วย
- **`App size` เป็น `Character size`** มันไม่เคยขยายทั้งแอป ขยายแค่ตัวละคร type ตามชื่อไป
  (`AppScale` → `CharacterScale`) แต่ key บนดิสก์ยังเป็น `appearance.appScale` เพราะ rename
  แล้วค่าที่คนเลือกไว้จะหายเงียบๆ

ขับจริงที่ v0.13.214–216: เพิ่ม AI-Secretary ให้ Ditto แล้วไฟล์ `projects-<uuid>.json` ของเธอ
โผล่มาพร้อม `claude.code` ส่วนของอาเนียยังว่างทั้งบนดิสก์และในแผง, ลบ project แล้ว
`swift-functional-programming` หายจากรายการทันทีโดยไม่ต้องกด ↻, ตั้ง Theme Light ให้ Ditto
แล้วอาเนียยังดำอยู่, ตั้ง Character size L ให้อาเนียแล้วหน้าต่างเธอโต 128×149 → 167×194
อีกสองตัวเท่าเดิม, ปิดเปิดแอปใหม่ทั้งสองค่ายังอยู่

## Phase 13-2 — Small improvement (v0.13.223)

- **Esc มีเจ้าของคนเดียวแล้ว** เดิมมีสามที่ที่ดัก Esc และตัวที่แย่ที่สุดคือ monitor ของ
  `ChatPanelView` ซึ่งติดตั้งครั้งเดียวและไม่เคยถูกถอด (view ถูกสร้างหนเดียวแล้วโชว์ด้วย alpha)
  มันจึงกลืน Esc ของทั้งแอปไม่ว่าแชทจะเปิดอยู่หรือไม่ ตอนนี้ตัดสินที่ `dismissDecision`
  ที่เดียว เป็นบันได: pinned pane → chat → ตัวละคร
- **กด Esc ตอนแชทปิดอยู่ = ซ่อนตัวละคร** และรังสุดท้ายนี้ไปถึงได้จาก local monitor เท่านั้น
  **เอาเข้า global claim ไม่ได้** เพราะตัวละครอยู่บนจอเกือบตลอดเวลา การ claim Esc ตอนเธอโชว์
  ก็คือ claim ถาวร ซึ่งเป็น regression ที่ `GlobalShortcut` เขียนใบเสร็จไว้แล้ว
- **สองทางยิงพร้อมกันไม่ได้** — local press จะคืนค่า nil ทันทีที่มีอะไรให้ dismiss เพราะนั่นคือ
  ตอนที่ hot key ถูก register อยู่พอดี เขียนเป็นกติกา ไม่ฝากไว้กับลำดับการ consume ของ event
- **บั๊กที่เทสบอกไม่ได้ เจอตอนขับ (0.13.223)**: ปิดแชทแล้วแอปยัง active แต่ **ไม่มี key window
  เลย** local monitor จึงไม่เห็นปุ่ม กด Esc ครั้งที่สองไม่เกิดอะไร แก้โดยให้แชทส่งคีย์บอร์ดต่อ
  ให้ character panel ตอนปิด — เป็นการ *ส่งต่อ* สิ่งที่ถืออยู่แล้ว ไม่ใช่ *แย่ง* มา ซึ่งเป็นเหตุผลที่
  character panel ยังไม่รับ key ตอนถูกคลิกเหมือนเดิม
- **⌘H = ทั้งแอปจริงๆ** เดิมซ่อนแค่ตัวละคร เหลือ pinned pane, Token Usage, About ค้างจอ
  ตอนนี้ไปหมด และเขียนเป็น `hide` ไม่ใช่ `toggle` เพราะ toggle ที่ยิงใส่ทั้ง roster จะ
  *เรียกคืน* คนที่ซ่อนอยู่แล้ว
- **ขากลับจำว่าซ่อนใครไป** reopen คืนเฉพาะคนที่ ⌘H เอาไป ไม่ใช่คืนแค่ `focused` ตัวเดียว
- **ข้อจำกัดที่ตั้งใจ**: ⌘H ทำงานเมื่อหน้าต่างของเราถือคีย์บอร์ดอยู่เท่านั้น การ claim ⌘H
  ทั้งระบบคือ regression อีกตัวที่เคยเกิดแล้ว — ไม่ทำ

ขับจริงที่ v0.13.223 (สามตัวละครบนจอ): คลิกตัวแรก → แชทเปิด 423×719, Esc#1 → แชทปิด
ตัวละครครบสาม, Esc#2 → **หายเฉพาะตัวที่คลิก** อีกสองตัวอยู่, Esc#3 → ไม่มีอะไรเกิดขึ้น,
เปิดแชท + ⌘U (Token Usage) แล้ว ⌘H → หน้าต่างบนจอเหลือศูนย์, reopen → กลับมาสองตัว
ที่ ⌘H เอาไป (ตัวที่ Esc ซ่อนยังซ่อนอยู่ ถูกต้อง), กด Esc ใน Finder → ไม่มีตัวละครหาย,
เปิดแชทแล้วกด Esc ใน Finder → แชทปิด ตัวละครอยู่ครบ (hot key ยังทำงาน แม้ตัวดักใน
`ChatPanelView` ถูกถอดออกแล้ว)

## Phase 13-3 — Performance Improvement (v0.13.224)

### ผลการวัด (2026-08-13, CLI 2.1.229, v0.13.223)

**แอปไม่ได้ช้า — spawn `claude` หลังกด Return แค่ 0.04 วินาที** วัดจากแอปที่รันอยู่จริง
โดยจับเวลาตั้งแต่ปุ่ม Return ถูกโพสต์จนลูกของ process แอปโผล่ใน `pgrep -P` เวลาที่เหลือ
ทั้งหมดเป็นของ Claude Code ไม่ใช่ของเรา สิ่งที่ต้องแก้จึงไม่ใช่โค้ดฝั่ง UI หรือ orchestration

| สิ่งที่วัด | first line | first text | total |
|---|---|---|---|
| CLI flags เดียวกับแอป (โฟลเดอร์เปล่า) | 0.81s | 2.92s | 3.17s |
| เพิ่ม `--strict-mcp-config` (ปิด MCP) | 0.89s | 3.30s | 3.56s |
| turn ใหม่ / `--resume` turn 2 / turn 3 | 1.0 / 1.4 / 0.9s | 5.08 / 5.05 / 6.09s | — |
| ในแอปจริง (turn เดียว trivial) | — | — | 5.92s |

**สมมติฐานที่ผิดและถูกตัดทิ้งด้วยการวัด:**
- **ไม่ใช่ MCP** — ปิดแล้วเวลาเท่าเดิม connector ที่ผูกกับ claude.ai ไม่ได้ถูกต่อใน headless run
- **ไม่ใช่ login shell** — `LoginShellPath.resolve()` cache ไว้แล้วจริง หนึ่งครั้งต่ออายุแอป
- **ไม่ใช่งานฝั่งเราก่อน spawn** — 0.04s

**ตัวการคือ process ใหม่ทุกเทิร์น** ทุกครั้งที่สั่งงาน แอปเปิด `claude -p --resume <id>` ใหม่
ต้อง boot node, โหลด config, แล้วอ่าน transcript เก่ากลับเข้ามาทั้งก้อน — cache ของ prompt
เย็นทุกรอบ

**ทางแก้ที่วัดแล้วว่าได้ผล: `--input-format stream-json`** ป้อน user message ทีละบรรทัดเข้า
stdin ของ process เดียวที่เปิดค้างไว้ วัดที่ system prompt ขนาดจริง (~6.3KB):

| | first text | total |
|---|---|---|
| turn 1 (process เพิ่งเปิด) | 5.47s | 5.73s |
| turn 2 (process เดิม) | **1.49s** | 2.22s |
| turn 3 (process เดิม) | **1.15s** | 2.10s |

**เร็วขึ้นราว 3.7 เท่า ประหยัดราว 4 วินาทีต่อเทิร์น**

**ข้อจำกัดที่ต้องออกแบบรอบ:** `--append-system-prompt`, working directory, `--allowedTools`,
`--add-dir`, `--model`, `--effort`, `--chrome` เป็น flag ตอน launch ทั้งหมด เปลี่ยนเมื่อไหร่
ต้องเปิด process ใหม่ กติกาว่า "เมื่อไหร่ต้องเปิดใหม่" จึงต้องเป็น pure function ใน library target
ตรวจแล้วว่าค่าเหล่านี้ **ไม่เปลี่ยนระหว่างเทิร์นปกติ** — `outstanding` (ตัวเดียวที่ทำให้ system
prompt ขยับกลางบทสนทนา) ถูกตั้งเฉพาะตอนเทิร์นถูก block เรื่อง permission ซึ่งเป็นจังหวะที่
`allowedTools` เปลี่ยนอยู่แล้ว จึงต้องเปิดใหม่อยู่ดี ไม่ได้เสียของ

**สี่อย่างที่ process ค้างต้องทำให้ได้เท่าของเดิม** (แต่ละข้อคือ failure mode ไม่ใช่ polish):
1. **กด Stop กลางเทิร์น** ของเดิม `process.terminate()` ได้เลย ของใหม่การฆ่า process คือ
   ฆ่าทั้ง session
2. **`.staleSession`** ของเดิมอ่าน stderr หลัง process ตาย process ที่ไม่ตายต้องมีทางอื่น
3. **เส้นแบ่งเทิร์น** ต้องอ่านจนถึง `type == "result"` แล้วหยุด ไม่งั้น event ของเทิร์นถัดไป
   จะไปลงบัวเก่า
4. **process ตายเงียบระหว่างเทิร์น** ต้องรู้ตอนเขียน stdin ไม่ใช่ค้างรอ

### สิ่งที่แก้

**เก็บ process ของ Claude Code ไว้ตัวละหนึ่งต่อ character แทนที่จะเปิดใหม่ทุกเทิร์น**
ป้อน user message ทีละบรรทัดเข้า stdin ด้วย `--input-format stream-json`

- **`WarmProcessKey` คือกติกาว่าเทิร์นนี้ใช้ process เดิมได้ไหม** อยู่ใน library target
  มีเทสของตัวเอง ทุกค่าในนั้นเป็น flag ตอน launch ที่เปลี่ยนบน process ที่รันอยู่ไม่ได้
  ต่างเมื่อไหร่ = เปิดใหม่ **การเทียบ key คือตัว invalidate เอง** ไม่ใช่ flag "stale" ที่
  call site ใหม่ในอนาคตจะลืมเรียก
- **process ที่สร้าง session เอง ต้องจำ id ทันทีที่ init event บอก** ไม่งั้นเทิร์นถัดไป
  (ซึ่งรู้ id แล้ว) จะไม่รู้จัก process ที่ตัวเองคุยอยู่ แล้วเปิดใหม่ทุกเทิร์นเงียบๆ
- **ข้อความของ user ไม่ได้อยู่บน command line อีกแล้ว** ลงไปเป็น JSON string ทาง stdin
  ซึ่งไม่มีใคร parse เป็น flag บั๊ก `- A` ที่เคยบังคับให้ต้องอยู่หลัง `--` จึง**หมดไป
  ไม่ใช่ถูกกัน** — `DashPromptTests` เขียนใหม่ให้ยืนยันบนเส้นทางใหม่ว่าข้อความเดิมยัง
  ส่งครบและไม่โผล่ใน argv เลย
- **สี่อย่างที่ process ไม่ตายต่อเทิร์นต้องทำให้ได้**: หยุดอ่านที่บรรทัด `result`
  (ไม่งั้น event ของเทิร์นถัดไปลงบัวเก่า), ดูด stderr ตลอดเวลา (pipe ที่ไม่มีใครอ่านเต็มแล้ว
  ลูกค้าง — process ที่อยู่ทั้งบ่ายเจอแน่), รู้ว่าตายตอนเขียน stdin แล้ว retry หนึ่งครั้ง,
  และปิดให้ชัดตอน Stop / ลบ character / quit เพราะไม่มีอะไรปิดให้แล้ว

ขับจริงที่ v0.13.224: สามเทิร์นติดกันใช้ child pid เดียวกัน (77600) เวลา **5.37s → 2.46s
→ 2.02s** สามบัวแยกกันถูกต้องไม่มี event ปนข้ามเทิร์น, กด Stop กลางคัน → คำตอบตัดที่ 289
หัวข้อกลับเป็น idle child ตายตามที่ออกแบบ, เทิร์นถัดจาก Stop → child ใหม่ (79129) และ
โมเดลยังจำเทิร์นที่ถูกตัดได้ (ตอบ 357), ⌘Q → ไม่มี process ค้าง

## Bug — แชทไหลหลุดขอบล่างตอน WORKING (v0.13.225)

เจ้าของรายงาน: ระหว่าง AI ทำงาน ข้อความในแชทขยับลงทีละนิด **เป็นช่วงๆ** ทั้งที่เลื่อนลงล่างสุดแล้ว
ผลคือไม่เห็นข้อความล่าสุดสักที

**ต้นเหตุ** ตัวสั่ง "เลื่อนตาม" คือ `onChange(of: transcriptSignature)` และ signature คือ
`จำนวนข้อความ : ความยาวข้อความสุดท้าย` ซึ่งกล่อง Working หลบได้ทั้งสองทาง: `recordActivity`
**แทรก entry ของมันไว้เหนือบับเบิลที่กำลังพิมพ์** มันจึงไม่ใช่ตัวสุดท้าย และทุก step ถัดมาก็เอา
activity ทั้งลิสต์มา join ทับลง entry เดิม จำนวนข้อความจึงไม่เปลี่ยนด้วย กล่องโตขึ้นบรรทัดละ step
ดันทุกอย่างใต้มันลง โดยไม่มีอะไรเลื่อนตาม — **"เป็นช่วงๆ" คือแต่ละ step ของ tool**
เกิดเฉพาะตอนเปิด "Showing what I'm doing" ซึ่ง default ปิด แต่เจ้าของเปิดอยู่

**สิ่งที่แก้** signature ต้องรู้ล่วงหน้าว่าอะไรทำให้จอขยับได้บ้าง ซึ่งเป็นข้อสอบที่มันสอบตกไปแล้ว
สองข้อ (อีกข้อคือการ์ดขออนุมัติที่โผล่กลางเทิร์นแล้วบีบ viewport ให้เตี้ยลง signature ก็ไม่ขยับ)
จึงเปลี่ยนไปตัดสินจาก **ตำแหน่งจริงของท้ายเนื้อหา** ที่ `TranscriptTailKey` วัดส่งมาทุก layout
pass อยู่แล้ว — `TranscriptScrollPin.isBehind(distanceBelowFold:)` และลบ signature ทิ้ง
**ตำแหน่งมองไม่พลาดว่าอะไรเป็นตัวดัน** เพราะไม่ต้องรู้ว่าอะไรดัน

- **สองค่านี้ต้องเป็นคนละเลข และเทสเขียนไว้ว่าทำไม** `tolerance` (24pt) คือ "ท้ายเนื้อหาหลุดขอบ
  ได้แค่ไหนโดยยังนับว่าคนอ่านอยู่ล่างสุด" ต้องใจกว้าง เพราะ token ลงก่อน layout pass ที่เลื่อนตาม
  หนึ่งจังหวะ ส่วน `settled` (0.5pt) คือ "ผิดที่ได้แค่ไหนก่อนจะถูกดันกลับ" ต้องเกือบศูนย์ เพราะที่
  ปล่อยไว้คือ drift ที่คนอ่านนั่งดูมันสะสม ใช้เลขเดียวกันคือต้องเลือกระหว่างกระชากคนที่เลื่อนขึ้นไป
  บรรทัดเดียว กับปล่อยให้คำตอบจมใต้ขอบไปบรรทัดหนึ่ง
- **การเลื่อนเปลี่ยนค่าที่วัดได้ แล้วค่านั้นก็วิ่งกลับเข้ามาที่เดิม และมันลู่เข้า** เพราะท้ายเนื้อหาที่
  นั่งพอดีขอบล่างแล้วไม่ขออะไรอีก
- **สิ่งที่การวัดยังห้ามทำเหมือนเดิมคือปิดการตามเอง** เนื้อหาที่โตใต้คนที่ไม่ได้ขยับ หน้าตาจาก
  ตรงนี้เหมือนกันเป๊ะกับคนเลื่อนหนีขึ้นไป การออกจากล่างสุดจึงยังเป็นเรื่องของ scroll wheel ที่เดียว

ขับจริงโดยเจ้าของที่ v0.13.225: อาการหายแล้ว 964 เทส

## Phase 13-4 — Font (v0.13.228)

สองข้อในสprint: ปรับน้ำหนัก font ในแชทที่ "เข้าใจว่าเป็น bold" และเลือก font family ได้
โดยเติมใน Settings ก่อนแถว Text size

### สิ่งที่วัดได้ก่อนแก้

**ไม่มีอะไรตั้ง bold เลยสักจุด** — `MessageTextView.styled` ทา
`monospacedSystemFont(weight: .regular)` ทับทั้งช่วงก่อนวนดู markdown ด้วยซ้ำ
ตัวการคือ **SF Mono ไม่มีอักษรไทย** ข้อความไทยจึงตกไปหาฟอนต์ถัดไปที่ระบบหาให้
วัดเมื่อ 2026-08-14 ได้ **Ayuthaya** ซึ่งเป็นหน้าตัวกว้างและหนา เทียบกับฟอนต์ระบบปกติที่ได้
ThonburiUI-Regular ที่บางกว่ามาก **การไปปรับ weight เป็น medium จึงแก้ไม่ตรงจุด** เพราะ
Ayuthaya ที่ weight regular ก็หนาเท่าเดิม สองข้อในสprint จึงเป็นเรื่องเดียวกัน คือหน้าตัวอักษร

### สิ่งที่แก้

**`FontChoice` สี่แบบของระบบ — System / Rounded / Serif / Mono — ไม่ใช่รายชื่อ family**
เพราะ system design ถูก resolve แยกตามภาษา ไทยจึงได้หน้าตาของ design นั้นเสมอ
ส่วนชื่อ family การันตีไม่ได้: ของที่ `availableFontFamilies` คืนมาครึ่งหนึ่งไม่มีอักษรไทย
การมี picker ที่เลือกแล้วข้อความของเจ้าของกลายเป็นกล่องเปล่า แย่กว่าบั๊กที่กำลังแก้อยู่

- **default คือ System** (ไม่ใช่ค่าเดิม ตกลงกับเจ้าของสดตอนทำ ต่างจากที่ sprint เขียนไว้ว่า
  "ใช้ค่าเดิมเป็น default") คนที่อัปเกรดมาไม่มีคีย์นี้ ก็ตกมาที่ System ด้วย
  **จงใจให้ fallback เป็นค่าใหม่ ไม่ใช่พฤติกรรมเดิม** เพราะ fallback เป็นของเดิมคือคงบั๊กที่เขาแจ้ง
- **code block ไม่เกี่ยวด้วย** fenced block ยัง monospace เสมอไม่ว่าเลือกอะไร เพราะการเรียง
  ตรงคอลัมน์คือความหมายของมัน ส่วนตาราง / ตัวเลือกใต้คำถาม ตามค่าที่เลือก เพราะเป็นเนื้อ
  บทสนทนาเหมือนกัน ตารางที่คนละหน้าตากับย่อหน้าเหนือมันอ่านเหมือนบั๊ก
- **เก็บแยกรายตัวละคร** ตามแพตเทิร์นเดิมทุกอย่าง: อ่านค่าร่วมจนกว่าจะมีของตัวเอง
  คีย์ชื่อ `fontDesign` (ไม่ใช่ `font` เพราะ tuple ของชื่อคีย์มีสมาชิก `font` ที่ชี้ไป `fontSize`
  อยู่แล้ว การตั้งชื่อซ้ำทำให้ `read(names.font)` กำกวม)
- **ผลวัดหลังแก้**: System / Rounded / Serif → ไทยได้ ThonburiUI-Regular และตัวหนาได้
  ThonburiUI-Bold ส่วน Mono ยังได้ Ayuthaya ตามเดิม ซึ่งถูกแล้วเพราะเป็นตัวเลือกที่คนเลือกเอง
  **ข้อจำกัดที่รู้ไว้: Mono ไม่มีหน้าตัวหนาของไทย `**ตัวหนา**` ในภาษาไทยจึงไม่เห็นความต่าง**

### บั๊กที่เจอระหว่างเขียนฟังก์ชันเดิมใหม่ และแก้ไปด้วย

`***ตัวหนาเอียง***` เสียความหนา — เดิมเป็น `if` สองอันที่ต่างเขียน font ทั้งตัวลงไป และ
สาขา italic รันทีหลังจึงทับสาขา bold ตอนนี้ตัดสิน weight กับ slant พร้อมกัน เขียนครั้งเดียวต่อ run
ตระกูลที่ไม่มีหน้าเอียง (Rounded) จะปล่อยตั้งตรงไว้ ไม่ปลอมเอียงให้

**ยังไม่ได้แก้ ตั้งใจ:** `` `inline code` `` ยังไม่ถูกจัดรูปเลย — `.inlinePresentationIntent`
มี `.code` ส่งมาจริง แต่ไม่มีสาขารับ เป็นการตัดสินใจเรื่องหน้าตาใหม่ ไม่ใช่ของแถมของรอบนี้

## Phase 14-1 / 14-2 — Character คุยกันเอง และส่งงานให้กันได้ (v0.14.229)

`SPRINT_BACKLOG.md` แยกไว้เป็นสองข้อ แต่ลงมือแล้วเป็นกลไกเดียวกัน จึง ship พร้อมกัน:
14.1 คือรู้จักกันและส่งข้อความหากันได้ 14.2 คือส่ง "งาน" แล้วได้คำตอบกลับ ซึ่งต่างกันแค่ว่า
ข้อความนั้นมีคำตอบผูกกลับมาหรือไม่

**อยู่ใน process เดียว ไม่ใช่หลาย process** Phase 13 ทำ `CharacterInstance` ไปแล้ว
ทุกตัวละครอยู่บน MainActor เดียวกัน การส่งจึงเป็นการเรียกฟังก์ชัน ไม่ต้องมีไฟล์กลาง
ไม่ต้องมี lock — ของพวกนั้นคือ Phase 14.3 / 17 และการสร้างมันตอนนี้คือจ่ายค่าอันตราย
ทุกข้อที่ 14.3 ระบุไว้ โดยยังไม่ได้ประโยชน์อะไรเลย

### ซองจดหมายคือข้อมูล ไม่ใช่สิทธิ์

`CharacterMessage` **ไม่มี path ไม่มี grant ไม่มี tool id ไม่มี session id** อาเนียทำงาน
ด้วย `ProjectRegistry` ของตัวเอง สิทธิ์ของตัวเอง session ของตัวเอง งานที่เอื้อมออกไปนอก
ของที่เธอมีอยู่แล้วจะถูกปฏิเสธ ไม่ใช่ถูกขยายสิทธิ์ให้เงียบๆ — ซึ่งคือเงื่อนไขที่เจ้าของเพิ่มใน 14.2
(`รู้ว่าอีกตัวทำงานกับ project ไหน แต่ไม่มีสิทธิ์`) เขียนลงไปใน *ชนิดข้อมูล* ไม่ใช่ในกฎ

`CharacterCard` ถือแต่ **ชื่อ** ของ project ไม่ถือ path เทสใช้ `Mirror` ตรวจรายชื่อฟิลด์
ของ `CharacterMessage` ไว้ **การเพิ่มฟิลด์ที่ชื่อมีคำว่า path/grant/tool/session จะทำให้เทสแดง**
แทนที่จะรั่วเงียบๆ

### อ่านร้อยแก้วได้ เพราะตอนไม่มั่นใจมันถาม

เจ้าของเลือกให้ app ตีความเอง (ไม่ใช้ fenced block) กติกาใน charter ที่ว่า
"ห้ามเดาจากร้อยแก้ว" จริงๆ แล้วปกป้องเรื่อง **ห้ามลงมือจากการเดา** ไม่ใช่ห้ามอ่าน
`delegationIntent` จึงตอบสามอย่าง และตัวกลางคือหัวใจ:

- วลีส่งต่อชัดเจน (`ขอให้`, `ฝากถาม`, `ask … to`) + ชื่อในทะเบียนพอดีหนึ่งชื่อ → ส่งเลย
- วลีส่งต่อ แต่ **ไม่เจอชื่อในทะเบียน** → **ถามว่าให้ใคร** นี่คือเคสที่สำคัญที่สุด: สคริปต์ของ
  เจ้าของเองเขียน "อาเนีย" ให้ตัวละครที่ชื่อ profile ว่า "Anya" ถ้าจับคู่ชื่อไม่ได้แล้วถือว่า
  ไม่เกี่ยวกับใคร มันจะตอบเองเงียบๆ ซึ่งคนอ่านว่า "ฟีเจอร์ไม่ทำงาน"
- สองชื่อในประโยคเดียว หรือชื่อ + คำกริยาอ่อน (`อาเนียบอกว่าอะไรนะ`) → ถาม
- ไม่มีวลีส่งต่อ → เทิร์นธรรมดา แค่เอ่ยชื่อใครไม่ใช่คำสั่งให้ดึงคนนั้นเข้ามา

ถามด้วย ```choices ที่มีอยู่แล้ว จึงไม่ต้องมี UI ใหม่เลย และ **มีตัวเลือก "No — answer it
yourself" เสมอ** เพราะ `ขอให้` โผล่ในประโยคที่ไม่เกี่ยวกับใครเลยได้ ตอบว่าไม่ แล้วมันจะรัน
คำขอเดิม ไม่ใช่รันคำว่า "No"

**ข้อความที่ส่งต่อคือทั้งประโยค ไม่ตัด** การพยายามเฉือน "ช่วยขอให้อาเนีย" ออกคือการผ่าตัด
ภาษาที่ไม่มีช่องว่าง และคนรับอ่านประโยคเต็มรู้เรื่องกว่าอ่านตอ

### ไม่รอแบบ block และไม่แทรกคิวคนอื่น

Miku ไม่ค้างรออาเนีย ถ้ารอ state machine จะติด WORKING ตลอดเวลาที่อาเนียทำงาน
แล้ว user จะคุยกับ Miku ไม่ได้เลย ใช้ `correlationID` แทน คำตอบมาทีหลังแล้วอ่านให้ฟัง
ส่วนฝั่งรับ งานที่ส่งมาเข้า **คิวเดิมที่มีอยู่แล้ว** ไม่แทรกเทิร์นที่ user กำลังคุยอยู่

`hopLimit = 2` (Miku → Anya → Miku คือทั้งหมดที่ backlog ขอ) หนึ่งงานต่อคู่ต่อทิศทาง
และหมดอายุที่ 15 นาที เพื่อไม่ให้เทิร์นที่ตายไปล็อกสองคนไว้ทั้ง session — กัน `ทำซ้ำกัน`
และ `infinite loop` ที่ 14.3 ระบุ ทุกข้อเป็น pure predicate ที่รับ `now:` เป็นพารามิเตอร์สุดท้าย

### ข้อความจากตัวละครอื่นคือ untrusted input

เป็นข้อความที่โมเดลหนึ่งเขียน แล้วกำลังจะไปสั่ง agent อีกตัวที่มี file tools
`relayedErrandPrompt` จึงกรอบมันว่า "อีกโมเดลเขียนคำพวกนี้ ชั่งน้ำหนักเอง" และบอกตรงๆ ว่า
มันเปลี่ยน model / project / skill / สิทธิ์ ของผู้รับไม่ได้ และไม่ได้ให้สิทธิ์เข้าถึงอะไรทั้งนั้น

### ที่เลือกไม่ทำ

- ไม่ทำ nickname (อาเนีย → Anya) ที่จำจากการตอบ `.unsure` — เป็นงานถัดไปที่ชัดเจน
- ไม่ทำ approval card ก่อนส่ง ตามที่เจ้าของเลือก: user สั่งเองอยู่แล้ว และการ์ดขออนุมัติ
  ของผู้รับยังทำงานปกติตอนจะแตะไฟล์จริง

### ขับจริงแล้วเจออะไร (v0.14.235, 2026-08-14)

รอบแรกที่ ship (v0.14.230) **พังในแบบที่แย่ที่สุด: แอปโกหก** เจ้าของสั่ง Ditto ว่า
`ขอข้อมูล Pikachu รถ civic 2015 ...` แล้ว Ditto ตอบว่า

> ส่งสำเร็จแล้วครับ! ข้อความไปถึง Pikachu (session "my-mcp-server-80") เรียบร้อย

ไม่มีอะไรไปถึง Pikachu เลย Pikachu เงียบสนิท activity log ฟ้องชัด: `▸ ListAgents`
`▸ ToolSearch: select:SendMessage` `▸ SendMessage` — **โมเดลไปหยิบเครื่องมือคุยข้าม
session ของ Claude Code เอง** ซึ่งมีจริงและทำงานจริง แต่มันคุยกับ *Claude Code session*
ไม่ใช่ตัวละครในแอป (`my-mcp-server-80` คือชื่อ session จริง) relay ที่เขียนไว้ไม่เคยถูกเรียกเลย
สักครั้งในบทสนทนานั้น

สี่อย่างที่ผิด แก้ครบแล้ว:

1. **เครื่องมือนั้นเอื้อมถึงได้** `--allowedTools` คือรายการ *auto-approve* ไม่ใช่การจำกัด
   ของที่ไม่อยู่ในรายการยังมีอยู่ แค่ต้องขออนุญาต → ใช้ `--disallowedTools` ปิด
   `SendMessage`, `ListAgents` ตายตัว **ไม่มีถ้อยคำใน prompt ที่ทำให้เครื่องมือซึ่งวางอยู่
   ตรงหน้าเลิกดูเหมือนคำตอบได้**
2. **prompt เชิญชวนให้ทำแบบนั้น** เดิมเขียนว่า "แอปจะส่งข้อความให้" แต่ไม่บอกว่าขอยังไง
   โมเดลเลยไปหาเอง ตอนนี้บอกตรงๆ ว่าเครื่องมือของตัวเองไม่มีอันไหนเอื้อมถึงตัวละครอื่นได้
   และ **ห้ามบอกผู้ใช้ว่าติดต่อใครแล้ว เว้นแต่ใช้ block และเห็นบรรทัดยืนยันของแอป**
3. **ไม่มี block ให้ใช้** การอ่านร้อยแก้วจับเคสตรงๆ ได้ แต่ตอนพลาด เทิร์นจะตกไปที่โมเดล
   ซึ่ง**ไม่มีทางที่ถูกต้องให้ทำสิ่งที่เพิ่งถูกสั่ง** เพิ่ม ```to แล้ว รูปแบบเดียวกับ watch / run /
   choices — marked เสมอ ไม่เดา ชื่อที่ไม่รู้จักตอบกลับว่ามีใครอยู่บ้าง ไม่จับคู่สะกดใกล้เคียง
4. **ตัวอ่านร้อยแก้วแคบเกิน** `ขอข้อมูล ... มาจาก Pikachu` เป็นการส่งต่อชัดๆ แต่ไม่ match
   อะไรเลย เพิ่มเข้าไปแล้ว โดยจัดเป็นชนิด "ถามก่อน" ไม่ใช่ "ส่งเลย"

**และอีกข้อที่เจอจากการ grep ไฟล์ที่เซฟไว้ ไม่ใช่จากการอ่านโค้ด**: ตัวละครที่ *รับ* งาน
มีบรรทัดอยู่บนดิสก์ทุกตัว ส่วนตัวละครที่ *ส่ง* ไม่มีสักตัว เพราะการส่งต่อจงใจไม่กินเทิร์นของผู้ส่ง
และคนที่เขียนไฟล์คือ `finishChat` บรรทัด "ส่งให้แล้ว" คำถามว่าให้ใคร และคำตอบที่กลับมา
จึงอยู่แค่ในหน่วยความจำจนกว่าจะปิดแอป — **"เห็นเป็นลายลักษณ์อักษรทั้งสองฝั่ง" คือทั้งหมด
ของฟีเจอร์นี้ และ "เห็นจนกว่าจะปิดแอป" ไม่ใช่สิ่งนั้น** ตอนนี้ file ทันทีที่เกิด

### ผลการขับรอบที่แก้แล้ว (ผ่าน)

ข้อความเดิมเป๊ะที่เคยพัง → `Should I pass this to Pikachu?` พร้อม picker
[Pikachu / No — answer it yourself] **ไม่กินเทิร์นโมเดล ไม่มีการแต่งเรื่อง**
เลือก Pikachu → `→ Passed this on to Pikachu. I'll say when there's an answer.`
Pikachu ขึ้น `← ... passed this on from you` ยิง MCP `query_rate_book` ของตัวเอง
แล้วคำตอบกลับเข้าแชทต้นทางว่า `← Pikachu answered:` พร้อมราคาประเมินจริง
(Vios ท็อป 203,000 / E 190,000 / J 184,000 บาท) ครบวงตามสคริปต์ใน backlog
และยืนยันบนดิสก์แล้วว่าทั้งสามบรรทัดถูกเซฟ

### ส่งหลายคนพร้อมกัน และทำ step ต่อเนื่อง (v0.14.239–240)

เจ้าของลองใช้จริงแล้วเจอสองข้อที่ทำไม่ได้ ทั้งคู่เป็นช่องโหว่ของดีไซน์ ไม่ใช่บั๊กเล็ก

**สองชื่อเคยแปลว่า "กำกวม"** `จาก Pikachu และ Ditto` คือคำขอเดียวถึงสองคน แต่ถูกอ่านว่า
"หมายถึงคนไหน" แล้วเด้ง picker ตอนนี้ **สองชื่อที่มีคำเชื่อม (`และ` / `กับ` / `and`) = ส่งทั้งคู่**
ส่วนสองชื่อที่ไม่มีคำเชื่อมยังถามเหมือนเดิม เพราะ `บอก Anya ว่า Ditto ทำเสร็จแล้ว` ไม่ใช่คำขอถึง Ditto
และไม่มีถ้อยคำอะไรแยกสองกรณีนี้ออกจากกันนอกจากคำเชื่อม

**คำขอที่มีเลขข้อคือแผน ไม่ใช่ข้อความ** เดิมส่งต่อทั้งก้อน ข้อ 2 จึงไปกับข้อ 1 ด้วย —
คนที่แค่ต้องตอบข้อ 1 ถูกสั่งให้ไปรวมไฟล์ในโปรเจกต์ที่เธอเข้าไม่ถึง ส่วนคนที่ต้องทำข้อ 2
ก็ยกงานตัวเองให้คนอื่นไปแล้ว ตอนนี้ข้อ 1 ออกไป ที่เหลือรออยู่ที่ผู้สั่ง พอคำตอบครบ
ผู้สั่งรันข้อ 2 เองโดยมีคำตอบทุกชุดยกมาให้ครบ

**ทำงานพร้อมกัน ไม่ใช่ทีละคน** วัดจากการขับจริง (2026-08-14): ส่งออก 17:15:55 พร้อมกัน
Pikachu ตอบ 17:16:23 (28 วิ) Ditto ตอบ 17:16:41 (46 วิ) แล้วสรุปรวม 17:16:42
ถ้าเป็น sequential ต้องใช้ ~74 วิ **ผลจริง 46 วิ** แต่ละตัวทำใน session ของตัวเอง

**ไม่รอค้าง** ส่งไม่ถึงเลย → ตัดออกจากแผนทันทีและบอกตรงนั้น ไม่ใช่รอหมดเวลาค่อยบอก
ส่งถึงแต่เงียบ → หมดความอดทนแล้วไปต่อด้วยเท่าที่ได้ พร้อมสั่งในพรอมต์ว่า
**ห้ามเอาข้อมูลชุดเดียวมาเสนอเป็นการเทียบสองชุด** `errandPatience` แยกออกมาให้ฉีดได้
ด้วยเหตุผลเดียวกับนาฬิกา: พฤติกรรมที่ต้องเทสคือ "ไม่มีใครตอบ" และเทสที่ต้องรอจริง 15 นาที
คือเทสที่ไม่มีใครรัน

**ไม่ว่าง ≠ ถูกเมิน** ปลายทางที่กำลังคุยกับเจ้าของอยู่ รับงานเข้าคิวอยู่แล้ว ซึ่งถูกต้อง —
คนที่นั่งอยู่ตรงหน้าไม่ได้ขอให้ถูกแทรก แต่เธอไม่เคยบอกกลับ และจากอีกฝั่ง "เข้าคิว" กับ "เงียบ"
หน้าตาเหมือนกันเป๊ะ ตอนนี้ตอบกลับทันทีว่ารับแล้วคิวที่เท่าไหร่ ผู้ส่งขึ้นให้เห็น และนาฬิกาเริ่มนับใหม่
เพราะการตัดงานที่เข้าคิวจริงทิ้งเพราะหมดเวลาคือความผิดพลาด

### บั๊กที่เจอจากการขับรอบนี้ (v0.14.240)

หน้าจอถูกหมด แต่ไฟล์ไม่ตรง: ผู้สั่งเซฟ 17:17 ส่วน Pikachu ค้างที่ 16:58 และ Ditto 16:33
ทั้งที่เพิ่งตอบไปเมื่อ 17:16

`worthArchiving` ถามว่า "มีใครพูดอะไรไหม" แต่คำว่า *ใคร* หมายถึง `.user` เท่านั้น
**ตัวละครที่ใช้ทั้งบทสนทนาไปกับงานของตัวอื่นไม่มี user turn เลยสักอัน** — คำขอมาจากแอป
ทุกบรรทัดเป็นของเธอเอง และเจ้าของเครื่องไม่เคยพิมพ์อะไรลงไปในนั้น ทั้งบทสนทนาจึงตกเกณฑ์
แล้วหายไปเฉยๆ **เป็นกระจกเงาของบั๊กที่แก้ไปตอน v0.14.235 (ฝั่งผู้ส่ง) รากเดียวกัน:
การเซฟผูกอยู่กับเทิร์นที่คนพิมพ์ ส่วน relay สร้างบทสนทนาที่ไม่มีใครพิมพ์**

### บันทึกนี้เคยไปอยู่ผิดไฟล์ (แก้ที่ v0.14.241)

หัวข้อ Phase 14-1/14-2 ข้างบนถูกเขียนครั้งแรกด้วย `cat >>` ที่รันตอน cwd อยู่ใน `code/`
จึงไปสร้าง `code/PRODUCT_BACKLOG.md` ขึ้นใหม่แทนที่จะต่อท้ายไฟล์จริง แล้วถูก commit ไปด้วย
ผลคือ backlog จริงไม่มีหัวข้อ Phase 14 เลย ส่วนหัวข้อย่อยที่เขียนทีหลังไปห้อยอยู่ใต้ Phase 13-4
(Font) ซึ่งอ่านแล้วเข้าใจผิดสนิท **ให้ `cd` ให้ครบทุกครั้ง อย่าพึ่ง cwd ที่ค้างมาจากคำสั่งก่อนหน้า**
— กติกาเดียวกับที่ Versioning and packaging เขียนไว้เรื่อง `package-app.sh` และพลาดซ้ำที่เดิม

### ส่งถึงหลายคนต้องได้ทั้งสองทาง (v0.14.242)

`ขอราคา city มือ 2 เทียบกับ civic ของปี 2020 จาก Pikachu และ Ditto` **ไปถึงแค่ Pikachu**
พังคนละชั้นสองจุดต่อกัน:

1. **ตัวอ่านร้อยแก้วไม่จับ** มี `ขอข้อมูล` กับ `มาจาก` แต่ไม่มี `จาก` เดี่ยวๆ ซึ่งเป็นคำที่แบก
   ความหมาย "จากเธอ" จริงๆ ในภาษาไทย ประโยคที่เอ่ยสองชื่อและเชื่อมด้วย `และ` จึงถูกอ่านว่า
   ไม่เกี่ยวกับใคร แล้วตกไปที่โมเดล
2. **โมเดลทำถูกแล้ว แต่ทำไม่จบ** ```to รับได้ชื่อเดียว block ที่เขียนออกมาจึงมีแต่ Pikachu
   แล้วมันบอกเจ้าของว่า **"ขอถามทีละคนก่อนค่ะ"** — นั่นคือมันอธิบายข้อจำกัดที่เรายัดให้
   ไม่ใช่การเลือกที่จะช้า

ตอนนี้ ```to รับเป็นลิสต์ (คั่นด้วย `,` / `และ` / `กับ` / `and`) และ prompt สั่งให้ระบุทุกคน
พร้อมกัน **ห้ามเสนอถามทีละคน** ชื่อที่ไม่มีบนเดสก์ท็อปไม่ทำให้คนที่ระบุถูกอดรับงาน —
คนที่อยู่ได้รับ ส่วนคนที่ไม่มีถูกบอกออกมาตรงๆ

`จาก` เดี่ยวๆ เป็นคำธรรมดา (`อ่านจากไฟล์`) จึงอยู่ในกลุ่ม "ถามก่อน" ไม่ใช่ "ส่งเลย" —
ได้แค่คำถามถ้าไม่มีคำเชื่อมระหว่างชื่อ

### Miku ตั้งเป็น female แต่ตอบ "ครับ" (v0.14.243)

เพศอยู่ใน prompt มาตลอด — descriptor เขียนว่า `a teenage girl` — แต่เป็น**ภาษาอังกฤษ
ซึ่งไม่มีอะไรเปลี่ยนต่อจากนั้น** ส่วนภาษาไทยผูกเพศไว้กับคำลงท้ายและสรรพนามแทบทุกประโยค
และ**ไม่มีที่ไหนบอกว่าสองอย่างนี้เกี่ยวกัน** โมเดลจึงตกไปที่ ครับ ซึ่งเป็นค่า default
ไม่ได้ "เลือก" อะไร — นั่นคือเหตุผลที่โปรไฟล์ตั้งเหมือนกันได้คนละผล (อาเนีย female → ค่ะ)

เขียนผลลัพธ์ให้ชัดแทนการให้เดา รวมอายุด้วย เพราะสรรพนามไทยไม่ได้ผูกแค่เพศ —
เด็ก 6 ขวบพูด `ดิฉัน` อ่านแล้วเหมือนใส่ชุด เด็กจึงได้ `หนู` ส่วน `other` คือ**ไม่ระบุจริงๆ**
ให้เลือกอย่างเดียวแล้วคงเส้นคงวา ไม่ใช่สลับ ครับ/ค่ะ ไปมา และย้ำว่ากติกานี้
**เปลี่ยนรูปคำเท่านั้น ไม่เปลี่ยนว่าพูดอะไรหรือทำงานมากแค่ไหน**

ขับจริงหลังแก้: Miku **ครับ 0 / ค่ะ 3 / คะ 1** และยืนยันว่า Pikachu ที่ตอบ ครับ ถูกอยู่แล้ว
เพราะตั้งไว้ male

### คำตอบไปถึงหน้าจอ แต่ไปไม่ถึงตัวละคร (v0.14.244)

ตัวละครสองตัวตอบกลับมา คำตอบขึ้นครบบนจอ แล้วพอสั่งให้สรุป Miku ตอบว่า
**"ยังไม่มีอะไรให้สรุปเลยค่ะ — เรายังไม่ได้คุยหรือทำงานอะไรกันในเซสชันนี้เลย"**
**เธอพูดถูกตามที่ถูกบอกมา**

คำตอบถูก append ลง `conversation` ซึ่งดูเหมือนได้บอกแล้ว แต่ไม่ใช่ —
`ClaudeCodeProvider.swift:153` ส่งให้ CLI **แค่ user message ล่าสุด** เพราะ Claude Code
ถือ thread เองและกลับเข้าไปด้วย `--resume` ที่เหลือใน array นั้นเป็นสมุดบัญชีของแอปเอง
คำตอบจึงไปอยู่บนจอ ไปอยู่ในไฟล์ และไม่ไปอยู่ในที่ที่โมเดลจะอ่าน

ตอนนี้รอไปกับข้อความถัดไปที่คุยกับเธอ — ไม่เปลืองเทิร์น ไม่มีข้อความโผล่มาลอยๆ
และถึงมือตอนที่ต้องใช้พอดี **ถือไว้ครั้งเดียว** ไม่งั้นจะรายงานคำตอบเดิมทุกครั้งที่ถูกถามอะไร
ส่วนคำตอบที่เป็นของแผน (คำขอมีเลขข้อ) ไม่ถือไว้ทางนี้ เพราะ follow-up ยกมาให้อยู่แล้ว

**นี่คือบั๊กตัวที่สามในฟีเจอร์นี้ที่รูปร่างเดียวกัน: สิ่งที่คนเห็นชัดๆ แต่ตัวละครไม่มีทางรู้**
ตัวแรกทำบรรทัดฝั่งผู้ส่งหายจากไฟล์ ตัวที่สองทำบทสนทนาฝั่งผู้รับหายทั้งอัน
ตัวนี้ทำให้เธอคุยเรื่องคำตอบที่พิมพ์อยู่ในแชทตัวเองไม่ได้

## Small improvement — Hide All / Show All (v0.14.245–246)

เมนูแถวเดียวใต้รายชื่อตัวละคร เป็น toggle ที่ตัดสินจาก**สิ่งที่อยู่บนจอจริง**
ไม่ใช่จากสถานะที่จำไว้

**มีตัวใดตัวหนึ่งโชว์อยู่ = Hide All** ไม่ได้ใช้การนับ ซึ่งเป็นวิธีที่นึกออกก่อนและผิด:
ถ้านับ (2 ใน 3 ซ่อน → ขึ้น Show) ตัวที่ยังเห็นอยู่ ซึ่งเป็นตัวที่คนอยากให้หายพอดี
จะค้างอยู่ใต้แถวที่เสนอจะเรียกคนกลับมา มีแต่เดสก์ท็อปที่ว่างจริงเท่านั้นที่พลิกเป็น Show All

**Show All คืนแชทที่เปิดอยู่ด้วย ไม่ใช่แค่ตัวละคร** การซ่อนเกือบทุกครั้งเกิดจากบับเบิลเกะกะ
การกดกลับจึงควรได้บทสนทนาเดิมคืน — Show ที่คืนแค่ตัวละครไม่ใช่สิ่งตรงข้ามของ Hide ที่เอาไปทั้งคู่
แต่**อันที่ไม่ได้เปิดไว้จะไม่เปิดเอง** หน้าต่างที่โผล่มาโดยไม่ได้ขอแย่กว่าหน้าต่างที่ปิดอยู่
จดว่าแชทไหนเปิดอยู่ตอนกำลังจะปิด เพราะเป็นจังหวะเดียวที่รู้ได้

ใช้กลไกเดียวกับ ⌘H ไม่สร้างนิยามที่สองของคำว่า "หายไป" และตัวละครที่ถูกสร้างตอนซ่อนอยู่
กลับมาพร้อมกัน ไม่ค้างแบบมองไม่เห็นโดยไม่มีแถวไหนเรียกคืนได้

**ไม่มีตัวละครเลย = ไม่มีแถวนี้และไม่มีเส้นคั่น** เส้นที่ไม่มีอะไรอยู่ใต้มันคือเส้นที่ขีดโดยไม่มีเหตุผล
เส้นคั่น (v0.14.246) มีเพราะแถวข้างบนคือหนึ่งแถวต่อหนึ่งตัวละคร ส่วนแถวนี้พูดถึงทุกตัวพร้อมกัน
ถ้าไม่มีเส้นมันอ่านเหมือนเป็นชื่อตัวถัดไปในลิสต์

กติกาอยู่ใน `allCharactersTitle(_:)` ที่ `SecretaryCore` ตามกติกา charter —
`AppDelegate` แค่เอาคำตอบไปใช้ ขับจริงครบวง: ซ่อนแล้วหน้าต่างเหลือ 0 อัน
กดกลับได้ตัวละคร 4 ตัวและแชทคืนที่ `1165,391 361x520` เท่าเดิมเป๊ะ

## Memory ของ project — อ่านได้อยู่แล้ว ที่ขาดคือเขียนกลับ (v0.14.248)

เจ้าของสั่งว่า "ให้ app สามารถเข้าถึง memory ของ project ได้ และ save memory ของ project
นั้นไว้ด้วย เพื่อให้มี context เมื่อ app ทำงานกับ project นั้น"

**ครึ่งแรกมีอยู่แล้ว และไม่มีใครรู้** วัดจริงกับ Claude Code 2.1.220 (2026-08-14): สั่ง `claude -p`
ด้วย flag ชุดเดียวกับที่แอปใช้ โดยตั้ง cwd เป็น project ที่ลงทะเบียนไว้ มันโหลด `CLAUDE.md`
ของ project **และ** `~/.claude/projects/<slug>/memory/MEMORY.md` เข้า context ให้เอง
แปลว่า "เข้าถึง memory ของ project" เป็นจริงตั้งแต่ Phase 5 ที่เริ่มชี้ backend ไปที่โฟลเดอร์ project
สิ่งที่ไม่มีคือครึ่งหลัง: ของที่ตัวละครรู้ระหว่างทำงาน ไม่เคยถูกเขียนกลับไปเลยสักครั้ง

**จึงเขียนลงที่เดิม ไม่สร้างที่เก็บที่สอง** ทางเลือกที่ถูกปฏิเสธคือเก็บใน Application Support
ข้างๆ conversation ซึ่งจะกลายเป็นว่าต้องยัดเข้า `--append-system-prompt` เอง ขณะที่ตัวละคร
ก็ยังอ่าน memory *อีกอัน* จากที่เดียวกันในเชิงความหมาย และ `claude` ในเทอร์มินัลของเจ้าของ
จะเห็นอันหนึ่งไม่เห็นอีกอัน — ที่เก็บเดียว อ่านได้ทั้งสองฝั่ง ผลข้างเคียงเขียนไว้ตรงๆ ไม่ซ่อน:
สิ่งที่ตัวละครจำ คือสิ่งที่เทอร์มินัลของเจ้าของจะอ่านเจอด้วย นั่นคือฟีเจอร์ และเป็นเหตุผลที่ต้องถามก่อนเขียน

**ชื่อโฟลเดอร์ต้องวัด ไม่ใช่เดา** `claudeProjectSlug(forPath:)` แปลงทุกอักขระที่ไม่ใช่
`[A-Za-z0-9-]` เป็นขีด **นับต่อ UTF-16 code unit** ซึ่งไม่มีทางเดาถูกจากตัวอย่างเดียว:

- `/…/A_b.c d,e-ก่ะZ9` → `…-A-b-c-d-e----Z9` — ไทยสาม scalar ได้สามขีด ถ้าเดินต่อ
  `Character` จะได้สอง เพราะ `ก่` เป็น grapheme เดียว
- `/…/em🎨x` → `…-em--x` — emoji ตัวเดียวได้ **สอง** ขีด เพราะอยู่นอก BMP ข้อนี้ตัด
  `unicodeScalars` ทิ้ง เหลือ UTF-16 ทางเดียว
- จุดกลายเป็นขีด ไม่ได้หายไป — path ที่มี `/.claude/` จึงได้ **สองขีดติด** ซึ่งแปลว่า
  "แทนที่ slash" จะเขียนผิดโฟลเดอร์ให้ทุก worktree
- ต้องคลาย symlink ด้วย `realpath(3)` ไม่ใช่ Foundation: ทั้ง `standardizedFileURL` และ
  `resolvingSymlinksInPath` คืน `/tmp` เพราะอันหลังคลายเสร็จแล้วตัด `/private` ทิ้งอีกที
  ซึ่งเป็นชิ้นส่วนที่ชื่อโฟลเดอร์สร้างมาจากมันพอดี

เขียนผิดโฟลเดอร์คือความผิดพลาดแบบเงียบ: ไฟล์ถูกเขียนจริง แต่ไม่มีใครอ่านตลอดไป

**ขอด้วย marker ไม่เดาจากร้อยแก้ว** ```remember บรรทัดแรกคือหัวข้อ ที่เหลือคือเนื้อ
โมเดลพิมพ์ "จำไว้แล้วครับ" ตลอดเวลา และ reply ที่แค่พูดแบบนั้นต้องไม่เขียนไฟล์ลง memory
ของเจ้าของ ที่เทอร์มินัลตัวเองจะอ่านซ้ำไปอีกเป็นเดือน

**แอปเป็นคนเขียน ไม่ใช่โมเดล** จึงไม่ต้องขยาย tool grant ใดๆ เลย: โฟลเดอร์ memory อยู่นอก
ทุก project ที่ลงทะเบียน และไม่เคยถูกส่งเข้า `--add-dir` ของที่ backend เอื้อมถึงไม่เปลี่ยนเลย
ตัวละครขอด้วย block แอปเป็นคนลงมือ หลังคนตอบตกลง

สามด่าน แต่ละด่านเป็นการปฏิเสธที่พูดออกมา ไม่ใช่การเงียบแล้วทิ้ง:

- **ไม่มี project เปิดอยู่** = ไม่มี project ให้ข้อเท็จจริงนั้นสังกัด cwd ตอนนั้นคือ scratch
  และ memory ที่ลงไปตรงนั้นคือ memory ที่สังกัด project ที่ไม่มีใครเลือก
- **โน้ตที่อ่านเป็นคำสั่ง ถูกปฏิเสธก่อนถึงการ์ด** นี่คือสิ่งเดียวที่ memory มีแต่ block อื่นไม่มี:
  มันคือข้อความที่โมเดลเขียน แล้วถูกอ่านเป็น context ทุกเทิร์นต่อจากนี้ ทั้งจากแอปและจาก
  เทอร์มินัลของเจ้าของ `instructionRisks` รู้จักรูปพวกนี้อยู่แล้ว จึงใช้ซ้ำ
- **`.localWrite` = ถามทุกครั้ง** approve-once ถูกพิจารณาแล้วไม่เอา เพราะ grant จะกลายเป็น
  "เขียนลง `~/.claude` ได้" ซึ่งไม่ใช่ project ที่เจ้าของอนุมัติ และโน้ตแต่ละอันคนละประโยคกัน

Index สำคัญกว่าที่คิด: มีแต่ `MEMORY.md` ที่ถูกโหลดเข้า context อัตโนมัติ ไฟล์โน้ตที่ไม่มีบรรทัด
ชี้อยู่ในนั้นคือไฟล์ที่ไม่มีใครเปิด `memoryIndex(existing:adding:)` จึง **แทนที่บรรทัดเดิม
ไม่ใช่ต่อท้าย** เพราะไฟล์โน้ตถูกทับตามชื่อ ถ้าต่อท้ายจะได้สองบรรทัดชี้ไฟล์เดียวกัน
โดยบรรทัดหนึ่งบรรยายข้อเท็จจริงที่ไม่มีอยู่แล้ว

ลำดับการเขียนเลือกไว้ ไม่ได้บังเอิญ: ไฟล์โน้ตก่อน index ทีหลัง — โน้ตที่ไม่มี index คือโน้ตที่
มองไม่เห็นและไม่ทำอันตราย ส่วน index ที่ชี้ไฟล์ที่ไม่เคยถูกเขียนคือ pointer ค้างในเอกสาร
ที่ถูกอ่านทุก session

ไฟล์: `SecretaryCore/ProjectMemory.swift` (ทั้งหมด pure), `ProjectMemoryStore.swift`
(แตะดิสก์อย่างเดียว รับ `home` เข้ามา ไม่อ่านเอง) และ `PlannedOperation.rememberNote`
1100 เทส

**ที่ยังไม่ได้ทำ ตั้งใจ:** ไม่มี UI ให้ดู/ลบ memory ในแอป — แก้ที่ไฟล์ตรงๆ หรือผ่าน `claude`
ในเทอร์มินัลได้ และตอนไม่มี project เปิดอยู่ก็ยังไม่มี memory ให้ใช้เลย ซึ่งเป็นคนละเรื่องกับ
คำขอรอบนี้

**คำบนการ์ดแก้ตอนขับจริง (v0.14.249)** รอบแรกการ์ดอ่านว่า "May I run `remember “…” for
my-mcp-server, in your Claude Code memory` **in my-mcp-server**?" — ชื่อ project สองครั้ง
ในประโยคเดียว เพราะประโยคที่ห่อมันอยู่เติม "in <project>" ให้อยู่แล้ว summary จึงเลิกใส่ชื่อเอง
และเปลี่ยนไปพูดสิ่งที่การ์ดพูดผิด: subtitle ของ `.localWrite` เขียนว่า "Writes files in the
project" ซึ่งไม่ใช่ที่ที่ของอันนี้ลง แต่ subtitle นั้นใช้ร่วมกับ local write ทุกอันและถูกสำหรับอันอื่น
คำแก้จึงอยู่ที่ summary ไม่ใช่ที่ `ActionClass`

**ขับจริงครบวง** (Pikachu, project `my-mcp-server`): สั่งจำ → การ์ดขึ้น → กด Approve →
ได้ `build-script-code-repo-root.md` กับบรรทัดใน `MEMORY.md` จริงที่
`~/.claude/projects/-Users-Olarn-Desktop-my-mcp-server/memory/` หัวข้อภาษาไทยรอด
ชื่อไฟล์ ASCII ถูกถอดออกมาได้ แล้ว **เปิด `claude -p` ใหม่ใน project นั้นแล้วมันอ่านบรรทัดนั้น
กลับมาให้ได้** ซึ่งเป็นข้ออ้างทั้งหมดของดีไซน์นี้ — ที่เก็บเดียว อ่านได้ทั้งแอปและเทอร์มินัล

**หัวข้อภาษาไทยล้วนเคยทับกันเอง (v0.14.250)** ชื่อไฟล์ถอดมาจากหัวข้อในช่วง ASCII เท่านั้น
หัวข้อที่ไม่มี ASCII เลยจึงไม่เหลืออะไรให้ตั้งชื่อ ตอนแรกใช้คำตายตัว `project-note.md`
ซึ่งแปลว่า**ทุกข้อเท็จจริงที่เป็นไทยล้วนจะถูกเก็บเป็นไฟล์เดียวกัน อันที่สองทับอันแรกเงียบๆ**
และเพราะบรรทัดใน index ถูกแทนที่ไปพร้อมกัน มันจะไม่ดูพังด้วยซ้ำ เจ้าของเขียนไทย นี่จึงไม่ใช่มุม
เปลี่ยนเป็น `note-<fingerprint ของหัวข้อ>` — หัวข้อเดิมยังได้ไฟล์เดิมเสมอ (ซึ่งเป็นคุณสมบัติที่
`memoryIndex` พึ่งอยู่) แต่คนละหัวข้อได้คนละไฟล์ เจอเพราะขับจริง ไม่ได้เจอจากการอ่านโค้ด

**ช่องเดียวที่เคยเงียบ ปิดแล้ว (v0.14.251)** การ์ดมีได้ทีละใบ ถ้า reply เดียวกันขอทั้งติดตั้ง
skill และขอจำด้วย โน้ตจะถูกทิ้งโดยไม่พูดอะไรเลย ซึ่งขัดกับกติกาของฟีเจอร์นี้เองที่เขียนไว้สามที่ว่า
"ปฏิเสธต้องพูดออกมา ไม่ใช่เงียบแล้วทิ้ง" ตอนนี้พูด และ**เกิดน้อยพอที่จะไม่มีใครเจอจากการใช้งาน
จริง** จึงต้องมีเทสคุม ไม่ใช่ปล่อยไว้

## Refactor ตามรายงานคุณภาพโค้ด 2026-08-14 (v0.14.252–)

รายงานอยู่ที่ `docs/2026-08-14-code-quality-review.md` ทำทีละข้อ ข้อละคอมมิต
เทสเดิมต้องผ่านโดยไม่ถูกแก้ ยกเว้นข้อที่รายงานระบุว่าเป็นบั๊ก

**margin ของ bubble มีเจ้าของเดียวแล้ว (v0.14.252)** รายงานอ่านว่าเป็นบั๊ก (8 กับ 16
ไม่ตรงกัน) แต่พอไล่จริง ตัวเลขไม่ได้ขัดกัน — 16 ของ max-width คือ 8 ของ clamp คูณสอง
(ขอบซ้าย+ขวา) ที่ผิดคือมันเป็นค่าคงที่สองตัวในสองไฟล์ที่ derive จากกันโดยไม่มีอะไรบอก
แก้ให้ `bubbleScreenMargin` อยู่ที่ `SecretaryCore/BubblePlacement.swift` ที่เดียว และ
max-width มาจาก `usableBubbleWidth` ที่คูณสองอย่างเปิดเผย พร้อมเทสที่ pin ว่า bubble
กว้างเท่า usable width พอดีจะไม่ถูก clamp — สองค่านี้จึง drift จากกันอีกไม่ได้ พฤติกรรม
เท่าเดิมทุกไบต์

**เอกสารเลิกบรรยาย Keychain ที่ไม่มีจริง (v0.14.253)** README ทั้งสองยังบรรยายซาก
ดีไซน์ API-key ชุดเก่า — โมดูล `Credentials/` กับ `ClaudeChatProvider` ที่ไม่มีอยู่ และประโยค
"Secrets live in the Keychain" ทั้งที่รีโปไม่มีโค้ด Keychain สักบรรทัด (แอปขับ Claude Code
ของเจ้าของ จึงไม่ถือ credential เอง — นั่นคือประโยคที่ถูก) module layout เพิ่ม `FunctionalCore`
ที่เป็นฐานของทุก target แต่ไม่เคยถูกเขียนถึง และ doc comment ใน `AppearanceSettings`
เลิกยกตัวอย่างด้วยข้อความ Keychain ที่ไม่มีใน UI จริง

**prompt construction แยกออกจาก Secretary แล้ว (v0.14.254)** ~370 บรรทัดของข้อความ
prompt กับกติกาประกอบมัน ย้ายไป `SecretaryPrompts.swift` เป็นค่าคงที่ใน `SecretaryPrompt`
กับ pure function (`agentSystemPrompt`, `chatOnlySystemPrompt`, `browserPromptNote`,
`agentPermissionNote`) — `Secretary` เหลือแค่ property บางๆ ที่รวบ state แล้วเรียก
เทสเดิม 1105 ผ่านโดยไม่ถูกแก้แม้แต่ตัวเดียว ซึ่งเป็นหลักฐานว่า prompt เท่าเดิมทุกไบต์
(รวม assertion แบบ contains 200+ จุดที่ pin ข้อความอยู่) และได้เทสใหม่ 7 ตัวที่เช็ค
*การตัดสินใจ* ของการประกอบ — ชิ้นไหนโผล่เมื่อไหร่ — ด้วย exact equality ในจุดที่ pin ได้
ตามที่รายงานติว่า contains ผ่านได้แม้ prompt บวมสองเท่า `Secretary.swift` 4,153 → 3,865

**decision ในชั้นแอปที่ coverage มองไม่เห็น ย้ายเข้า library แล้วห้าตัว (v0.14.255)**
`AISecretaryApp` ไม่เคยถูก link เข้า test bundle กติกาของ charter คือ rule ที่แอปต้อง*ตัดสิน*
ต้องเป็น pure function ใน library target ห้าตัวที่รายงานชี้ ย้ายไป
`SecretaryCore/WindowGeometry.swift` + `AppearanceSettings`: clamp ขนาด info window,
cascade ต้นทางหน้าต่าง (พร้อมเทสว่าตัวที่เก้าวนกลับ), clamp ความสูง message box,
`ChatResizeDrag` — สูตร resize + sign flip ที่ doc comment บันทึกบั๊ก oscillation
(909 → 801 → 933 → 777) ไว้แต่ไม่เคยมีที่ให้เขียนเทส ตอนนี้มี regression test แล้ว —
และ `captionFontSize` แทนสูตร `max(9, secondaryFontSize - 2)` ที่เขียนซ้ำหกที่ใน
`UsageWindow` view เหลือแค่ apply คำตอบ เทสใหม่ 11 ตัว ของเดิมผ่านหมดไม่ถูกแก้

**`ReplyRun` เป็น value แท้แล้ว (v0.14.256)** struct ห้า `var` ที่ถูก mutate ผ่าน `inout`
ในแปด handler — ตัวอย่างที่ใช้ปิดช่องของสกิล §2 เอง — เปลี่ยนเป็น `let` ทั้งหมด เปลี่ยนสถานะ
ด้วยเมธอด `-ing` (`closingSegment`, `appending`, `noting`, `inSegment`,
`afterMovingToWorking`) แต่ละ handler รับ run ปัจจุบันคืน run ถัดไป เหลือตัวแปรเดียวคือ
accumulator ของ loop ที่อ่าน stream เทสเดิม 1,123 ผ่านโดยไม่ถูกแก้

**`ChatPanelView.swift` แยกเป็นหกไฟล์ตาม seam ของมันเอง (v0.14.257)** 2,114 บรรทัด
ไฟล์เดียว → core 474 + `+Header` (badges ทั้งห้า), `+Transcript` (scroller กับ markdown
rendering), `+Composer` (message box, attachments, ↵), `+Panels` (Settings/Projects/
Skills/footer), `+Keys` (arrow-key owner, history recall, monitors) การย้ายเป็น verbatim
พิสูจน์ด้วย line-multiset diff: ต่างกันแค่วงเล็บของ extension, doc comment หัวไฟล์ และ
access level ที่ต้องเปิดข้ามไฟล์ (`@State` ทุกตัวยังประกาศที่ struct — Swift ไม่ให้ประกาศใน
extension) `private` ที่เหลือในแต่ละไฟล์แปลว่า "ใช้ในไฟล์นี้เท่านั้น" ซึ่งตอนนี้เป็นจริงและอ่านออก

**`.toOptional()` ใน `Secretary.swift` จาก 57 เหลือ 10 (v0.14.258)** ตามข้อ 1 ของรายงาน:
`Option` ที่ทุกจุดอ่านถูกแกะเป็น `guard let` คือ type ที่เป็นแค่เอกสาร แปลงตามรูปแบบของสกิล —
เปรียบเทียบใช้ `== .some(x)`, predicate ใช้ `map(...)^.getOrElse(false)` (ตัวซ้ำสามที่ได้ชื่อ
`instructionRunIsRunning`), การกระทำเมื่อมีค่าใช้ `fold` โดยแตก body ยาวเป็นฟังก์ชันครึ่งหลัง
(`startWatch`, `askForPlan`, `sendForUnderstanding`, `perform(step:)` ฯลฯ) และ `Either`
ที่เคยถูกยุบเป็น Option ทิ้ง error (`read.toOption().toOptional()` + `swap()` อ่าน error
กลับทีหลัง) กลายเป็น `fold` ที่อ่านทั้งสอง rail ตรงๆ สิบจุดที่เหลือแต่ละจุดมีเหตุผลเชิงโครงสร้าง
ที่ Bow 0.8 ไม่มีทางเลี่ยง: `guard case` ห้าจุด (Option เป็น class จึง pattern-match enum
ข้างในไม่ได้), ขอบ API ที่รับ Optional ของ Swift, closure ของ `Task` ที่ต้อง `let self`,
`continue` ใน loop ที่ mutate ตาม index, และ decision ladder สองจุดที่ early-return สามชั้น
เทสเดิม 1,123 ผ่านโดยไม่ถูกแก้ทุก checkpoint (build+test หลังทุก batch)

## Phase 14.3 — ปิดโดยไม่มีโค้ดใหม่

**ไม่มีอะไรถูกสร้างเพิ่มสำหรับเฟสนี้เลย** และนั่นคือบันทึก ไม่ใช่การข้าม สองข้อที่ 14.3 ขอ
ถูกทำไปแล้วตอน 14.1/14.2 เพราะทั้งคู่เป็นเงื่อนไขที่ต้องมีตั้งแต่ตอนที่ตัวละครส่งงานหากันได้จริง
ไม่ใช่ของที่ค่อยเติมทีหลังได้:

- **ทำ parallel ได้กี่ตัว / รออะไรจึงรู้ว่าเสร็จ** — `ErrandPlan` (v0.14.239–240) ส่งพร้อมกัน
  รอครบ แล้วรันขั้นถัดไปที่ฝั่งผู้สั่ง พร้อม timeout และการตัดคนที่ติดต่อไม่ได้ออกจากการรอ
- **กัน `ทำซ้ำกัน` / `infinite loop`** — `hopLimit = 2`, หนึ่งงานต่อคู่ต่อทิศทาง, หมดอายุ
  15 นาที ทุกข้อเป็น pure predicate ที่รับ `now:` เป็นพารามิเตอร์สุดท้าย (v0.14.229)

**สิ่งที่ยังไม่มี และเป็นการอ่านอีกแบบของข้อแรก**: กลไกพวกนี้ *ระบบ* รู้ แต่ *คนดูจอไม่เห็น* —
ไม่มีที่ไหนบนหน้าจอบอกว่าตอนนี้ส่งไปกี่คน ใครตอบแล้ว ใครยังไม่ตอบ ถ้าเจ้าของหมายถึงการเห็น
ไม่ใช่การรู้ นั่นคืองานใหม่ที่ยังไม่ได้ทำ และควรเป็นหัวข้อของมันเอง ไม่ใช่ติ๊กย้อนหลังลงช่องนี้

ส่วนคำถามเรื่อง shared instruction file / task log ข้ามโปรเซส ไม่ได้อยู่ในเฟสนี้ —
อยู่ที่ `PRODUCT_BACKLOG_NEXT_SPRINTS.md` และยังติดด่าน `## TODO: Design App
Architecture ก่อน` ที่เจ้าของต้องลบเอง

## Phase 14.4 — ตัวละครหายใจตอนคิด (v0.14.259–260)

**สิ่งที่ ship จริง อ่านตรงนี้พอ**: ระหว่าง `thinking` และ `working` วงกลมที่ครอบตัวละคร
กระพริบจากสีเทาไปเป็นสีของสถานะ และ status badge ขยาย/ย่อ 10% ในจังหวะเดียวกัน
รอบละ 1.2 วินาที พอไม่ busy ทุกอย่างหยุดสนิทและกลับเป็นสีของสถานะนั้นตามเดิม
สองย่อหน้าถัดไปเป็นของรอบ 259 ซึ่ง**ถูกแทนที่บางส่วนแล้ว** — รอบนั้นกระพริบแค่ badge
สลับขั้วสีตรงข้ามกับตอนนี้ และมีบั๊กที่หยุดไม่ได้ หัวข้อย่อย v0.14.260 ท้ายสุดคือของจริง

status badge ของตัวละครบอกสถานะด้วยสีกับไอคอนอยู่แล้ว แต่นิ่งสนิท — จอที่กำลังคิดอยู่กับจอที่
คิดเสร็จแล้วต่างกันแค่สีม่วงกับสีเทา ซึ่งอ่านไม่ออกจากหางตา ตอนนี้ระหว่าง `thinking` และ
`working` badge จะขยาย/ย่อ 10% และสีพื้นวงจะเข้ม/จางไปพร้อมกัน จังหวะละ 1.2 วินาที

**ตัวเลขไม่ได้อยู่ในไฟล์ view** เพราะ `AISecretaryApp` ไม่เคยถูกลิงก์เข้า test bundle ตามที่
charter ระบุ กฎว่า "สถานะไหนหายใจ หายใจแรงแค่ไหน เร็วแค่ไหน" อยู่ที่ `StatusPulse` กับ
`statusPulse(for:)` ใน `AssistantState` ส่วน view แค่เอาคำตอบไปใช้ — เทสหกตัวจึงพูดถึง
กฎได้ตรงๆ รวมถึงข้อที่สำคัญกว่าตัวการเคลื่อนไหวเอง คือ **สถานะที่ไม่ busy ต้องนิ่ง** เพราะ
badge ที่ยังขยับหลังคำตอบมาถึงแล้วอ่านว่างานยังไม่จบ

สามอย่างที่เลือกแบบนี้ด้วยเหตุผล ไม่ใช่ค่าที่หยิบมา:

- **จางเฉพาะสีพื้น ไม่จางทั้ง badge** — ที่ขอคือสีเข้ม/จาง ถ้าลด opacity ทั้งอันวงขาวกับไอคอน
  จะจางตาม แล้วมันอ่านเป็น badge กำลังจะดับ ไม่ใช่กำลังหายใจ
- **ผูกกับ `isBusy` ไม่ใช่ตารางรายสถานะ** — `thinking` กับ `working` คือช่วงเดียวกันที่คนนั่งรอ
  ตารางที่มีแถวต่อสถานะคือแถวที่ต้องมีคนจำว่าต้องเพิ่ม เจ้าของเขียนคำขอไว้กับ thinking
  ส่วน working ให้จังหวะเดียวกัน (สมมติฐานที่บอกไว้ตรงนี้ ไม่ใช่เดาเงียบๆ)
- **flag เดียวคุมทั้งขนาดและสี** — สองflag หรือสอง animation จะเลื่อนหลุดจากกันภายในไม่กี่รอบ
  และ `ตรงกับจังหวะ pulse` ก็หายไป

`isAnimated` ต้องการทั้งการเคลื่อนไหวและเวลา: pulse ที่ period เป็น 0 หรือที่ scale/opacity
เป็น 1 ทั้งคู่ ถ้าส่งให้ `repeatForever` จะได้ animation ที่วนตลอดกาลโดยไม่แสดงอะไรเลย

ยังไม่ได้ทำ: `prefers-reduced-motion` ของ macOS (`NSWorkspace.accessibilityDisplayShouldReduceMotion`)
ยังไม่ถูกอ่าน — เป็นงานเล็กแต่เป็นคนละเรื่อง และควรครอบทุก animation ในแอปพร้อมกัน

### กระพริบทั้งวง สลับขั้วสี และเลิกใช้ `repeatForever` (v0.14.260)

รอบ 259 **ship ทั้งที่มีบั๊ก** และเจอจากการวัด ไม่ใช่จากการดูภาพ: badge ยังหายใจต่อหลัง
กลับเป็น idle แล้ว จับได้ด้วยการ capture หน้าต่างตัวละครสามครั้งห่างกันหนึ่งวินาทีตอนไม่มีงาน
แล้ว diff — บริเวณ badge ต่างกันทุกคู่ สาเหตุคือ `repeatForever` ที่ผูกกับ `@State` bool:
animation ที่วนไม่รู้จบมีอายุยืนกว่าค่าที่จุดมันติด พอ flag กลับเป็น false มันก็ยังวนอยู่
**ภาพนิ่งใบเดียวพิสูจน์ไม่ได้ทั้งว่ามันขยับและว่ามันหยุด** ต้องวัดหลายเฟรม

เจ้าของขอเพิ่มสองข้อในรอบเดียวกัน และทั้งสองข้อเปลี่ยนกลไก ไม่ใช่แค่ค่าคงที่:

- **กระพริบทั้งวงที่ครอบตัวละคร ไม่ใช่แค่ไอคอน** — halo 104pt คือของที่ใหญ่ที่สุดบนเดสก์ท็อป
  ที่เปลี่ยนสีได้ วงไม่ขยาย/ย่อ เพราะกรอบที่เปลี่ยนขนาดจะลากขอบตัวละครไปด้วย
- **สลับขั้ว** — ขนาดปกติคือสีเทา ตอนขยายคือสีของสถานะ (เดิมกลับกัน: สีเต็มตอนปกติ จางตอนขยาย)

กลไกใหม่คือ `TimelineView` + **ฟังก์ชันบริสุทธิ์ของเวลา** `pulseProgress(_:at:)` แทน flag
ที่ถูก animate ซึ่งได้สามอย่างพร้อมกัน:

1. **หยุดได้จริง** — `.still` คืน 0 ทุกค่าเวลาที่เป็นไปได้ ไม่มี state ค้างให้ติด และมีเทสที่
   เดินเวลา 0–5 วินาทียืนยันข้อนี้ ซึ่งเป็นเทสที่เขียนก่อนเพื่อน เพราะมันคือบั๊กที่เพิ่ง ship ไป
2. **halo กับ badge อยู่ในจังหวะเดียวกันโดยโครงสร้าง** — ต่างคนต่างอ่านนาฬิกา แต่เป็นฟังก์ชัน
   ของเวลาอย่างเดียว input เดียวกันจึงได้ค่าเดียวกันเสมอ ไม่มี phase ที่แชร์กันให้เลื่อนหลุด
3. **ไม่กิน CPU ตอนว่าง** — `paused:` ตัด redraw ทิ้งทั้งหมดเมื่อไม่ busy

`halo` เป็น `TimelineView` ของตัวเองที่ครอบแค่วงกลม ไม่ครอบ `characterArt` เพราะ art
อ่านรูปจากดิสก์ทุกครั้งที่ถูกสร้างใหม่ — ครอบรวมกันคือการอ่านไฟล์ 30 ครั้งต่อวินาที

**ที่วัดได้ตอนขับจริงรอบ 259** เส้นกลางแนวนอนของ badge ระหว่าง `working`: เส้นผ่านศูนย์กลาง
แกว่ง 47 ↔ 52 px = 1.106 เท่า ตรงกับ 1.1 ที่ตั้งไว้ และ diff ระหว่างเฟรมพิสูจน์ว่าบริเวณเดียว
ในหน้าต่างที่เปลี่ยนคือ badge — ซึ่งตอนนั้นถูกต้อง สิ่งที่ผิดคือมันไม่ยอมหยุด

## Small improvement — Model/Effort ที่ยังไม่ได้เลือก อ่านว่า "Default" (v0.14.261)

แถว Model ในแผง Profile เขียนว่า **"Unknown"** เมื่อยังไม่มีใครเลือก model และแอปยังอ่าน
ไม่ได้ว่า Claude Code ของเจ้าของจะหยิบตัวไหน ซึ่งไม่มีอะไรพัง — ที่จะรันคือค่า default ของ
Claude Code เอง แต่ **"Unknown" อ่านเป็นความผิดพลาด** เหมือนตั้งค่าอะไรไว้ไม่ครบ
และมันอยู่เหนือเมนูที่เขียนคำตอบที่ถูกต้องไว้อยู่แล้วว่า "Your Claude Code default"

เปลี่ยนเป็น **"Default"** และเปลี่ยนแถว Effort ด้วยทั้งที่เจ้าของสั่งมาแค่ Model —
สองแถวนี้อยู่ติดกันในแผงเดียวกัน ใช้กลไก fallback เดียวกัน และหมายความเหมือนกันเป๊ะ
ถ้าแก้แถวเดียวจะกลายเป็นสองคำสำหรับสถานการณ์เดียว ซึ่งอ่านเป็นคนละเรื่อง คำนี้จึงอยู่ที่
`Secretary.inheritedName` ที่เดียว ไม่ใช่ literal สองตัว

เทสสองตัว: ตัวแรกปักว่าอ่านว่า "Default" ทั้งสองแถวเมื่อยังไม่ได้เลือกอะไร ตัวที่สองปักว่า
พอเลือกจริงแล้วต้องขึ้นชื่อรุ่นของมันเอง — เพราะ fallback ที่กลืนเคสที่มันมีไว้รองรับ
คือบั๊กที่เทสตัวแรกตัวเดียวมองไม่เห็น

## กติกาคอมเมนต์: why กับ warning เท่านั้น (v0.14.262)

เจ้าของสั่งสามข้อ: เขียนกฎลง charter, ทำโค้ดให้ self-documenting ตรงที่อ่านไม่รู้เรื่อง,
แล้วลบคอมเมนต์ที่ไม่ใช่ why/warning ออกทั้งหมด กฎอยู่ใน `CLAUDE.md` หัวข้อ
"Comments — why และ warning เท่านั้น" พร้อมสามเส้นแบ่งที่ต้องตัดสินจริง (ครึ่ง what ครึ่ง why
ให้เขียนใหม่ไม่ใช่ลบ, `///` บน public เก็บบรรทัดสรุปหนึ่งบรรทัดเพราะเป็นสัญญาของ interface,
บันทึกบั๊กคือ why เก็บทุกอัน) สองข้อหลังเป็นการตัดสินของเจ้าของเอง ไม่ใช่ที่ผมเดา

**ผลของการกวาดทั้งรีโป — 132 ไฟล์ 6,808 บรรทัดคอมเมนต์ — คือ 33 ไฟล์ +81/−173 และ
ไม่มีบรรทัดโค้ดเปลี่ยนเลยแม้แต่บรรทัดเดียว** (ยืนยันด้วยการกรอง diff เอาบรรทัดที่ไม่ใช่คอมเมนต์
ออกแล้วเหลือศูนย์) จำนวนน้อยเพราะโค้ดเขียนตามกฎนี้อยู่แล้วเกือบทั้งหมด สิ่งที่ตัดจริงมีสามแบบ:

- **คอมเมนต์ที่บรรยายซ้ำชื่อ** เช่น `/// A copy of these grants with one more pair approved.`
  บน `granting(projectID:toolID:)` หรือ `/// Nothing stands in the way; run it.` บน `.allowed`
- **บรรทัดสรุปที่มีย่อหน้า why ตามหลังอยู่แล้ว** — ตัดบรรทัดแรกทิ้ง เหลือเหตุผล
- **ประโยคปิดท้ายว่า "Split out of ChatPanelView.swift … nothing here changed in the move"**
  ทั้งห้าไฟล์ เป็นบันทึกเหตุการณ์ ไม่ใช่ why และเป็นข้อความที่จะเก่าโดยไม่มีใครรู้

**ของที่เจอโดยไม่ได้ตั้งใจหา และเป็นเหตุผลที่กฎนี้มีค่า: doc comment สี่จุดติดอยู่ผิดตัว**
เพราะบล็อกสองอันถูกเขียนติดกันโดยไม่มีบรรทัด `///` คั่น แล้วอันบนกลืนอันล่าง —
`tableView` ไปอยู่บน `codeView`, `messageField` ไปอยู่บน `messageBox`,
doc ของคลาส `UsageWindow` ไปอยู่บน `UsageRoster`, และ doc ของ `open(_:)` ไปอยู่บน
`applyControlAppearance` ทั้งสี่จุดคือคอมเมนต์ที่อ่านแล้วเชื่อได้แต่ผิด ซึ่งแย่กว่าไม่มีคอมเมนต์
ตรงตามเหตุผลที่เขียนไว้ในกฎเป๊ะ

ข้อ 2 (self-documenting) แทบไม่มีอะไรต้องแก้ **และนั่นคือผลการตรวจ ไม่ใช่การข้าม** — ไม่พบ
ชื่อไหนที่มีคอมเมนต์อยู่เพื่อชดเชยชื่อที่ไม่สื่อ ที่ใกล้เคียงที่สุดถูกแก้ไปในรอบก่อนแล้ว
(`Secretary.inheritedName` แทน literal "Default" สองตัว) ส่วนที่เหลือเป็นการรวบประโยค
what+why ให้เหลือประโยคเดียวที่ยังบอกเหตุผล

## ลากไฟล์มาวางตรงไหนของหน้าต่างก็ได้ (v0.14.263)

เดิม drop target คือกล่องพิมพ์อย่างเดียว ซึ่งเป็นแถบเตี้ยๆ ที่ก้นหน้าต่างที่เหลือทั้งหมด
เป็นบทสนทนา **ไฟล์ที่ปล่อยพลาดไปสองพิกเซลคือไฟล์ที่คนเชื่อว่าส่งไปแล้ว** ตอนนี้ทั้งฟองแชท
รับไฟล์ (`dropDestination` ย้ายไปอยู่หลัง `.background` ของ `SpeechBubbleShape` เพื่อให้
ขอบเขตคือรูปทรงที่ถูกเติมสี ไม่ใช่เฉพาะที่ที่บังเอิญมีเนื้อหา)

สิ่งที่บอกเรื่องนี้บนจอคือ **พื้นที่วางที่โผล่เหนือกล่องพิมพ์ระหว่างลากเท่านั้น** และหายไปเอง
เมื่อวางเสร็จ กรอบประ + สีเน้น + ไอคอน ส่วนการแสดง chip ของไฟล์ที่แนบแล้วเหมือนเดิมทุกอย่าง

สองอย่างที่ตัดสินแบบนี้ด้วยเหตุผล:

- **วางไว้เหนือกล่อง ไม่ใช่ตีกรอบรอบกล่อง** — กรอบรอบกล่องพิมพ์สื่อตรงข้ามกับสิ่งที่เพิ่งแก้
  คือมันชี้ว่า "ต้องเล็งมาตรงนี้" ซึ่งเป็นความเชื่อที่การเปลี่ยนรอบนี้ตั้งใจลบทิ้ง
- **พื้นที่วางไม่รับ hit-test เลย** (`allowsHitTesting(false)`) เพราะมันนั่งอยู่ในบริเวณที่
  ตัวมันเองโฆษณา ถ้ามันดักตัวชี้ คำว่า "ตรงไหนก็ได้" จะเป็นเท็จตรงจุดที่ประกาศคำนั้นพอดี

**ขับจริงแล้ว และต้องเขียนเครื่องมือใหม่ถึงจะขับได้** พื้นที่วางมีอยู่เฉพาะระหว่างลาก ส่วน
`drag.swift` เดิมกด–ลาก–ปล่อยรวดเดียว จึงไม่เหลือจังหวะให้ถ่ายภาพสิ่งเดียวที่ต้องตรวจ
`dragcap.swift` (เพิ่มลง `scripts/uidrive/`) ลากไฟล์จริงจาก Finder แล้ว **ค้างไว้พร้อมถ่ายภาพ**
ก่อนปล่อย ผลที่ได้: ลากไฟล์ CSV ไปกลางบทสนทนา — ห่างจากกล่องพิมพ์มาก — พื้นที่วางโผล่
เหนือกล่องพร้อมกรอบประและข้อความ "Drop a file — anywhere in this window" พอปล่อยแล้ว
พื้นที่หายทันทีและ chip `droptest.csv` ขึ้นในกล่องเหมือนเดิมทุกอย่าง

รอบแรกลากผิดของ: ใช้ `position of item` ของ Finder บวกกับ `bounds` ของหน้าต่าง แล้วได้พิกัด
ที่ตกในแถบข้าง จึงลากคำว่า "Recents" ไปวางแทน (แอปไม่รับ ซึ่งถูกแล้ว) วิธีที่ใช้ได้จริงคือ
capture หน้าต่าง Finder แล้วอ่านพิกัดไอคอนจากภาพ — บันทึกไว้ใน README ของสคริปต์แล้ว

### Shift+Enter ตัดบรรทัดตรงเคอร์เซอร์ และกล่องโตได้ 10 บรรทัด (v0.14.264)

เจ้าของรายงานว่า **กด Shift+Enter กลางประโยคแล้วบรรทัดไม่ถูกตัดตรงนั้น** แต่ไปขึ้นบรรทัดใหม่
ที่ท้ายข้อความแทน สาเหตุอยู่ในบรรทัดเดียว: `draft.append("\n")` ซึ่งต่อท้ายเสมอ ไม่เคยรู้ว่า
เคอร์เซอร์อยู่ไหน

แก้โดยส่งให้ **field editor ของ AppKit** เป็นคนแทรก (`insertNewlineIgnoringFieldEditor`)
ไม่ใช่ splice สตริงเอง เหตุผลไม่ใช่แค่ความสั้น: `selectedRange` ของ `NSTextView` นับเป็นหน่วย
UTF-16 การเอาเลขนั้นมาสร้าง `String.Index` เองมีโอกาสตกกลาง grapheme cluster
แล้วตัดอักษรไทยหรืออิโมจิขาดครึ่ง — AppKit รู้เรื่องนี้อยู่แล้ว ส่วน `insertNewline` เฉยๆ ใช้ไม่ได้
เพราะมันจบการแก้ไข ซึ่งเป็นเหตุผลเดียวกับที่ต้องดัก Return ตั้งแต่แรก

`append` ยังอยู่ในฐานะทางถอย กรณีหา field editor ไม่เจอ **มันคือบั๊กเดิม และเขียนไว้ตรงๆ
ว่าเป็นบั๊กเดิม** แต่ตัดบรรทัดผิดที่ยังดีกว่าปุ่มที่กดแล้วไม่เกิดอะไร

พร้อมกันนั้นขยาย `inputLineLimit` จาก 5 เป็น 10 ตามที่สั่ง — ค่านี้ถูกส่งเป็นพารามิเตอร์ให้
`messageBoxHeight(draft:lineHeight:lineLimit:)` อยู่แล้ว และเทสของมันส่งเลขของตัวเอง
จึงไม่มีเทสไหนต้องแก้ตาม ซึ่งเป็นเหตุผลที่ค่านั้นไม่ได้ถูกฝังไว้ในฟังก์ชันตั้งแต่แรก

**รอบ 264 หา field editor ไม่เจอ จึงตกลง fallback ทุกครั้ง (แก้ที่ v0.14.265)** เขียนไว้ว่า
`NSApp.keyWindow?.firstResponder as? NSTextView` ซึ่งอ่านแล้วถูก แต่ **panel นี้เป็นแบบ
non-activating**: แอปมักไม่ใช่แอปที่ active อยู่ `keyWindow` จึงเป็น nil ทั้งที่กล่องข้อความ
ถือเคอร์เซอร์และรับคีย์อยู่จริง ผลคือโค้ดวิ่งเข้า `draft.append("\n")` เหมือนเดิมทุกครั้ง
เปลี่ยนเป็นกวาด `NSApp.windows` หา first responder ที่เป็น `NSTextView` **และ `isEditable`**
— ตัวกรองนั้นจำเป็น เพราะ pane ที่ปักหมุดออกมาจากบทสนทนาก็เป็น `NSTextView` เหมือนกัน
และเป็น last first responder ของหน้าต่างตัวเองได้ แต่พิมพ์ลงไปไม่ได้

### ช่องว่างประหลาดกลางข้อความต้อนรับ (v0.14.266)

เจ้าของเห็นจากหน้าจอว่า `Ready — I'll work through your own Claude Code (2.1.233).` มี
ช่องว่างยาวๆ ก่อน `Add a project` **ในซอร์สมีเว้นวรรค 13 ตัวอยู่จริง** เป็นเศษของบรรทัดใหม่ที่ถูก
ยุบเข้ามาในประโยคเดียว แล้ว indent ค้างอยู่ ตอบคำถามที่ถามมา: ควรขึ้นบรรทัดใหม่ และตอนนี้เป็น
ย่อหน้าเว้นบรรทัดแล้ว เพราะสองประโยคนี้เป็นคนละจังหวะ — อันแรกบอกว่าฉันคืออะไร อันที่สอง
คือสิ่งเดียวที่ขอให้ทำ

**ย้ายข้อความออกจากไฟล์ view ไปเป็นฟังก์ชันบริสุทธิ์** `emptyTranscriptHint(_:)` กับ
`BackendReadiness` ใน `SecretaryCore` เพราะบั๊กนี้คือราคาของกฎที่ charter เขียนไว้พอดี:
`AISecretaryApp` ไม่เคยถูกลิงก์เข้า test bundle ช่องว่าง 13 ตัวจึงไม่มีอะไรมองเห็นได้เลย
นอกจากคนเปิดหน้าต่างดู เทสสี่ตัว **ปักทั้งสตริงไม่ใช่ `contains`** เพราะการเช็คว่า "มีคำนี้อยู่"
มองไม่เห็นช่องว่าง และมีตัวหนึ่งที่เดินทุกสถานะแล้วยืนยันว่าไม่มีเว้นวรรคซ้อนที่ไหนเลย —
สาเหตุคือบรรทัดที่ถูกยุบ ซึ่งไม่ได้ผูกกับข้อความอันไหนเป็นพิเศษ

`BackendReadiness` มีสามสถานะไม่ใช่สองแฟล็ก เพราะตัวกลางคือทั้งหมดของเรื่อง: **"ยังหาไม่เจอ"
ไม่เท่ากับ "ไม่มี"** และแผงที่อ่านสองอันนี้เป็นอันเดียวกันจะบอกให้ทุกคนที่เพิ่งเปิดแอปไปติดตั้ง
สิ่งที่เขามีอยู่แล้ว

**เว้นบรรทัดเดียวพอ (แก้ที่ v0.14.267)** รอบ 266 ผมเลือก `\n\n` เป็นย่อหน้า เจ้าของดูจากจอ
แล้วทักว่าเหมือนกด Enter สองรอบ ซึ่งถูก — ระหว่างสองประโยคสั้นๆ บรรทัดว่างอ่านเป็นช่องโหว่
ไม่ใช่โครงสร้าง เปลี่ยนเป็น `\n` เดียว และเพิ่มเทสที่เดินทุกสถานะยืนยันว่าไม่มี `\n\n` ที่ไหนเลย
คู่กับตัวที่กันเว้นวรรคซ้อนอยู่แล้ว — **ระยะห่างของข้อความนี้ถูกปักไว้ทั้งสองทางแล้ว** ทั้งแบบ
ติดกันเกินไปและห่างเกินไป

**ขับด้วยสคริปต์ไม่ได้ และรู้ว่าทำไม** `modkey <pid> 36 shift` ส่งคีย์ถึงแอปจริง แต่ SwiftUI
`onKeyPress(.return)` ไม่เห็น shift ที่มากับ synthetic event ผลคือกดแล้วไม่เกิดอะไรเลยทั้งใน
264 และ 265 ซึ่ง **อ่านเหมือนโค้ดพัง ทั้งที่เป็นเครื่องมือขับที่มองไม่เห็น** เจ้าของทดสอบด้วย
คีย์บอร์ดจริงแล้วยืนยันว่าใช้ได้ (2026-08-17) — บทเรียนเดียวกับ `.onExitCommand` และ
`.onKeyPress` กับปุ่มลูกศร: บางคีย์ต้องกดด้วยมือถึงจะรู้

**ข้อความในพื้นที่วางเป็นฟังก์ชันบริสุทธิ์ `attachmentDropPrompt(attached:limit:)` ใน
`SecretaryCore`** ไม่ใช่ literal ในไฟล์ view เพราะมันมีการตัดสินจริงอยู่: หน้าต่างรับ drop
ได้ทุกที่ คนจึงถือไฟล์ที่ 6 ลอยเหนือรายการที่เต็มแล้วได้ ถ้าพื้นที่ยังเขียนว่า "วางได้" มันคือ
การเชิญให้วางสิ่งที่ `admitting` จะปฏิเสธ แล้วคำปฏิเสธมาถึงหลังจากปล่อยมือไปแล้ว —
บอกก่อนปล่อยคือที่เดียวที่ช่วยได้ เทสสี่ตัว รวมตัวที่เดินทุกจำนวนตั้งแต่ 0 ถึงเกินลิมิตแล้วยืนยันว่า
**ข้อความกับ `admitting` ตอบตรงกันทุกค่า** ซึ่งเป็นสิ่งที่กันไม่ให้ทั้งสองฝั่งเลื่อนหลุดจากกันทีหลัง
